<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>前端面试常见</title>
    <url>/2020/06/03/%E5%89%8D%E7%AB%AF%E9%9D%A2%E8%AF%95%E5%B8%B8%E8%A7%81/</url>
    <content><![CDATA[<h4 id="1-请你谈谈Cookie的弊端？"><a href="#1-请你谈谈Cookie的弊端？" class="headerlink" title="1.请你谈谈Cookie的弊端？"></a>1.请你谈谈Cookie的弊端？</h4><p>cookie虽然在持久保存客户端数据提供了方便，分担了服务器存储的负担，但还是有很多局限性的。</p>
<p>第一：每个特定的域名下最多生成20个cookie</p>
<p>1.IE6或更低版本最多20个cookie</p>
<p>2.IE7和之后的版本最后可以有50个cookie。</p>
<p>3.Firefox最多50个cookie</p>
<p>4.chrome和Safari没有做硬性限制</p>
<p>IE和Opera 会清理近期最少使用的cookie，Firefox会随机清理cookie。</p>
<p>cookie的最大大约为4096字节，为了兼容性，一般不能超过4095字节。</p>
<p>IE 提供了一种存储可以持久化用户数据，叫做 userData，从IE5.0就开始支持。每个数据最多128K，每个域名下最多1M。这个持久化数据放在缓存中，如果缓存没有清理，那么会一直存在。</p>
<p>优点：极高的扩展性和可用性</p>
<p>1.通过良好的编程，控制保存在cookie中的session对象的大小。</p>
<p>2.通过加密和安全传输技术（SSL），减少cookie被破解的可能性。</p>
<p>3.只在cookie中存放不敏感数据，即使被盗也不会有重大损失。</p>
<p>4.控制cookie的生命期，使之不会永远有效。偷盗者很可能拿到一个过期的cookie。</p>
<p>缺点：</p>
<p>1.<code>Cookie</code>数量和长度的限制。每个domain最多只能有20条cookie，每个cookie长度不能超过4KB，否则会被截掉。</p>
<p>2.安全性问题。如果cookie被人拦截了，那人就可以取得所有的session信息。即使加密也与事无补，因为拦截者并不需要知道cookie的意义，他只要原样转发cookie就可以达到目的了。</p>
<p>3.有些状态不可能保存在客户端。例如，为了防止重复提交表单，我们需要在服务器端保存一个计数器。如果我们把这个计数器保存在客户端，那么它起不到任何作用。</p>
<h4 id="2-简单说一下浏览器本地存储是怎样的？"><a href="#2-简单说一下浏览器本地存储是怎样的？" class="headerlink" title="2.简单说一下浏览器本地存储是怎样的？"></a>2.简单说一下浏览器本地存储是怎样的？</h4><p>在较高版本的浏览器中，js提供了sessionStorage和globalStorage。在HTML5中提供了localStorage来取代globalStorage。</p>
<p>html5中的Web Storage包括了两种存储方式：sessionStorage和localStorage。</p>
<p>sessionStorage用于本地存储一个会话（session）中的数据，这些数据只有在同一个会话中的页面才能访问并且当会话结束后数据也随之销毁。因此sessionStorage不是一种持久化的本地存储，仅仅是会话级别的存储。</p>
<p>而localStorage用于持久化的本地存储，除非主动删除数据，否则数据是永远不会过期的。</p>
<h4 id="3-web-storage和cookie的区别？"><a href="#3-web-storage和cookie的区别？" class="headerlink" title="3.web storage和cookie的区别？"></a>3.web storage和cookie的区别？</h4><p>Web Storage的概念和cookie相似，区别是它是为了更大容量存储设计的。Cookie的大小是受限的，并且每次你请求一个新的页面的时候Cookie都会被发送过去，这样无形中浪费了带宽，另外cookie还需要指定作用域，不可以跨域调用。</p>
<p>除此之外，Web Storage拥有setItem,getItem,removeItem,clear等方法，不像cookie需要前端开发者自己封装setCookie，getCookie。</p>
<p>但是Cookie也是不可以或缺的：Cookie的作用是与服务器进行交互，作为HTTP规范的一部分而存在 ，而Web Storage仅仅是为了在本地“存储”数据而生</p>
<p>浏览器的支持除了IE７及以下不支持外，其他标准浏览器都完全支持(ie及FF需在web服务器里运行)，值得一提的是IE总是办好事，例如IE7、IE6中的UserData其实就是javascript本地存储的解决方案。通过简单的代码封装可以统一到所有的浏览器都支持web storage。</p>
<p>localStorage和sessionStorage都具有相同的操作方法，例如setItem、getItem和removeItem等</p>
<h4 id="3-1display-none和visibility-hidden的区别？"><a href="#3-1display-none和visibility-hidden的区别？" class="headerlink" title="3.1display:none和visibility:hidden的区别？"></a>3.1display:none和visibility:hidden的区别？</h4><p>display:none  隐藏对应的元素，在文档布局中不再给它分配空间，它各边的元素会合拢，</p>
<p>就当他从来不存在。</p>
<p>visibility:hidden  隐藏对应的元素，但是在文档布局中仍保留原来的空间。</p>
<h4 id="4-CSS中-link-和-import-的区别是？"><a href="#4-CSS中-link-和-import-的区别是？" class="headerlink" title="4.CSS中 link 和@import 的区别是？"></a>4.CSS中 link 和@import 的区别是？</h4><p>(1) link属于HTML标签，而@import是CSS提供的;</p>
<p>(2) 页面被加载的时，link会同时被加载，而@import引用的CSS会等到页面被加载完再加载;</p>
<p>(3) import只在IE5以上才能识别，而link是HTML标签，无兼容问题;</p>
<h4 id="4-1position的absolute与fixed共同点与不同点"><a href="#4-1position的absolute与fixed共同点与不同点" class="headerlink" title="4.1position的absolute与fixed共同点与不同点"></a>4.1position的absolute与fixed共同点与不同点</h4><p>共同点：</p>
<p>1.改变行内元素的呈现方式，display被置为inline-block；</p>
<p>2.让元素脱离普通流，不占据空间；</p>
<p>3.默认会覆盖到非定位元素上</p>
<p>不同点：</p>
<p>absolute的”根元素“是可以设置的，而fixed的”根元素“固定为浏览器窗口。</p>
<p>当你滚动网页，fixed元素与浏览器窗口之间的距离是不变的。</p>
<h4 id="5-介绍一下CSS的盒子模型？"><a href="#5-介绍一下CSS的盒子模型？" class="headerlink" title="5.介绍一下CSS的盒子模型？"></a>5.介绍一下CSS的盒子模型？</h4><p>1）盒模型： 内容(content)、填充(padding)、边界(margin)、 边框(border)</p>
<p>2）有两种， IE 盒子模型、标准 W3C 盒子模型；IE的content部分包含了 border 和 padding;</p>
<h4 id="6-CSS-选择符有哪些？哪些属性可以继承？优先级算法如何计算？CSS3新增伪类有那些？"><a href="#6-CSS-选择符有哪些？哪些属性可以继承？优先级算法如何计算？CSS3新增伪类有那些？" class="headerlink" title="6.CSS 选择符有哪些？哪些属性可以继承？优先级算法如何计算？CSS3新增伪类有那些？"></a>6.CSS 选择符有哪些？哪些属性可以继承？优先级算法如何计算？CSS3新增伪类有那些？</h4><p>CSS 选择符：</p>
<p>1.id选择器(# myid)</p>
<p>2.类选择器(.myclassname)</p>
<p>3.标签选择器(div, h1, p)</p>
<p>4.相邻选择器(h1 + p)</p>
<p>5.子选择器(ul &gt; li)</p>
<p>6.后代选择器(li a)</p>
<p>7.通配符选择器( * )</p>
<p>8.属性选择器(a[rel = “external”])</p>
<p>9.伪类选择器(a: hover, li:nth-child)</p>
<p>可继承的样式：</p>
<p>1.font-size</p>
<p>2.font-family</p>
<p>3.color</p>
<p>4.text-indent</p>
<p>不可继承的样式：</p>
<p>1.border</p>
<p>2.padding</p>
<p>3.margin</p>
<p>4.width</p>
<p>5.height</p>
<p>优先级算法：</p>
<p>1.优先级就近原则，同权重情况下样式定义最近者为准;</p>
<p>2.载入样式以最后载入的定位为准;</p>
<p>3.!important &gt;  id &gt; class &gt; tag  </p>
<p>4.important 比 内联优先级高，但内联比 id 要高</p>
<p>CSS3新增伪类举例：</p>
<p>p:first-of-type 选择属于其父元素的首个 <p> 元素的每个 <p> 元素。</p>
<p>p:last-of-type  选择属于其父元素的最后 <p> 元素的每个 <p> 元素。</p>
<p>p:only-of-type  选择属于其父元素唯一的 <p> 元素的每个 <p> 元素。</p>
<p>p:only-child   选择属于其父元素的唯一子元素的每个 <p> 元素。</p>
<p>p:nth-child(2)  选择属于其父元素的第二个子元素的每个 <p> 元素。</p>
<p>:enabled :disabled 控制表单控件的禁用状态。</p>
<p>:checked     单选框或复选框被选中</p>
<h4 id="7-列出display的值，说明他们的作用。position的值，-relative和absolute分别是相对于谁进行定位的？"><a href="#7-列出display的值，说明他们的作用。position的值，-relative和absolute分别是相对于谁进行定位的？" class="headerlink" title="7.列出display的值，说明他们的作用。position的值， relative和absolute分别是相对于谁进行定位的？"></a>7.列出display的值，说明他们的作用。position的值， relative和absolute分别是相对于谁进行定位的？</h4><p>display 的值的作用：</p>
<p>1.block 象块类型元素一样显示。</p>
<p>2.inline 缺省值。象行内元素类型一样显示。</p>
<p>3.inline-block 象行内元素一样显示，但其内容象块类型元素一样显示。</p>
<p>4.list-item 象块类型元素一样显示，并添加样式列表标记。</p>
<p>position 的值的定位区别：</p>
<p>1.absolute 生成绝对定位的元素，相对于 static 定位以外的第一个祖先元素进行定位。</p>
<p>2.fixed 生成固定定位的元素，相对于浏览器窗口进行定位（老IE不支持）。</p>
<p>3.relative 生成相对定位的元素，相对于其在普通流中的位置进行定位。</p>
<p>4.static 默认值。没有定位，元素出现在正常的流中（忽略 top, bottom, left, right z-index 声明）。</p>
<p>5.inherit 规定从父元素继承 position 属性的值。</p>
<h4 id="8-CSS3有哪些新特性？"><a href="#8-CSS3有哪些新特性？" class="headerlink" title="8.CSS3有哪些新特性？"></a>8.CSS3有哪些新特性？</h4><ol>
<li><p>CSS3实现圆角（border-radius），阴影（box-shadow），</p>
</li>
<li><p>对文字加特效（text-shadow、），线性渐变（gradient），旋转（transform）</p>
</li>
<li><p>transform:rotate(9deg) scale(0.85,0.90) translate(0px,-30px) skew(-9deg,0deg);// 旋转,缩放,定位,倾斜</p>
</li>
<li><p>增加了更多的CSS选择器  多背景 rgba</p>
</li>
<li><p>在CSS3中唯一引入的伪类是 ::selection.</p>
</li>
<li><p>媒体查询，多栏布局</p>
</li>
<li><p>border-image</p>
</li>
</ol>
<h4 id="9-为什么要初始化CSS样式。"><a href="#9-为什么要初始化CSS样式。" class="headerlink" title="9.为什么要初始化CSS样式。"></a>9.为什么要初始化CSS样式。</h4><p>因为浏览器的兼容问题，不同浏览器对有些标签的默认值是不同的，如果没对CSS初始化往往会出现浏览器之间的页面显示差异。</p>
<p>当然，初始化样式会对SEO有一定的影响，但鱼和熊掌不可兼得，但力求影响最小的情况下初始化。</p>
<h4 id="10-对BFC规范的理解？"><a href="#10-对BFC规范的理解？" class="headerlink" title="10.对BFC规范的理解？"></a>10.对BFC规范的理解？</h4><p>BFC，块级格式化上下文，一个创建了新的BFC的盒子是独立布局的，盒子里面的子元素的样式不会影响到外面的元素。在同一个 BFC 中的两个毗邻的块级盒在垂直方向（和布局方向有关系）的 margin 会发生折叠。</p>
<p>W3C CSS 2.1 规范中的一个概念，它决定了元素如何对其内容进行布局，以及与其他元素的关系和相互作用。</p>
<h4 id="11-解释下-CSS-sprites，以及你要如何在页面或网站中使用它。"><a href="#11-解释下-CSS-sprites，以及你要如何在页面或网站中使用它。" class="headerlink" title="11.解释下 CSS sprites，以及你要如何在页面或网站中使用它。"></a>11.解释下 CSS sprites，以及你要如何在页面或网站中使用它。</h4><p>CSS Sprites 其实就是把网页中一些背景图片整合到一张图片文件中，再利用 CSS 的”background-image”，”background-repeat”，”background-position” 的组合进行背景定位，background-position 可以用数字能精确的定位出背景图片的位置。这样可以减少很多图片请求的开销，因为请求耗时比较长；请求虽然可以并发，但是也有限制，一般浏览器都是6个。对于未来而言，就不需要这样做了，因为有了 http2。</p>
<h4 id="12-说说你对语义化的理解？"><a href="#12-说说你对语义化的理解？" class="headerlink" title="12.说说你对语义化的理解？"></a>12.说说你对语义化的理解？</h4><ol>
<li><p>去掉或者丢失样式的时候能够让页面呈现出清晰的结构</p>
</li>
<li><p>有利于SEO：和搜索引擎建立良好沟通，有助于爬虫抓取更多的有效信息：爬虫依赖于标签来确定上下文和各个关键字的权重；</p>
</li>
<li><p>方便其他设备解析（如屏幕阅读器、盲人阅读器、移动设备）以意义的方式来渲染网页；</p>
</li>
<li><p>便于团队开发和维护，语义化使得网页更具可读性，是进一步开发网页的必要步骤，遵循W3C标准的团队都遵循这个标准，可以减少差异化。</p>
</li>
</ol>
<h4 id="13-Doctype作用-严格模式与混杂模式如何区分？它们有何意义"><a href="#13-Doctype作用-严格模式与混杂模式如何区分？它们有何意义" class="headerlink" title="13.Doctype作用? 严格模式与混杂模式如何区分？它们有何意义?"></a>13.Doctype作用? 严格模式与混杂模式如何区分？它们有何意义?</h4><ol>
<li><!DOCTYPE><p> 声明位于文档中的最前面，处于 <html> 标签之前。告知浏览器以何种模式来渲染文档。 </p>
</li>
<li><p>严格模式的排版和 JS 运作模式是  以该浏览器支持的最高标准运行。</p>
</li>
<li><p>在混杂模式中，页面以宽松的向后兼容的方式显示。模拟老式浏览器的行为以防止站点无法工作。</p>
</li>
<li><p>DOCTYPE不存在或格式不正确会导致文档以混杂模式呈现。</p>
</li>
</ol>
<h4 id="14-你知道多少种Doctype文档类型？"><a href="#14-你知道多少种Doctype文档类型？" class="headerlink" title="14.你知道多少种Doctype文档类型？"></a>14.你知道多少种Doctype文档类型？</h4><ol>
<li><p>该标签可声明三种 DTD 类型，分别表示严格版本、过渡版本以及基于框架的 HTML 文档。</p>
</li>
<li><p>HTML 4.01 规定了三种文档类型：Strict、Transitional 以及 Frameset。</p>
</li>
<li><p>XHTML 1.0 规定了三种 XML 文档类型：Strict、Transitional 以及 Frameset。</p>
</li>
<li><p>Standards （标准）模式（也就是严格呈现模式）用于呈现遵循最新标准的网页，而 Quirks（包容）模式（也就是松散呈现模式或者兼容模式）用于呈现为传统浏览器而设计的网页。</p>
</li>
</ol>
<h4 id="15-HTML与XHTML——二者有什么区别"><a href="#15-HTML与XHTML——二者有什么区别" class="headerlink" title="15.HTML与XHTML——二者有什么区别"></a>15.HTML与XHTML——二者有什么区别</h4><p>最主要的不同：</p>
<p>XHTML 元素必须被正确地嵌套。</p>
<p>XHTML 元素必须被关闭。</p>
<p>标签名必须用小写字母。</p>
<p>XHTML 文档必须拥有根元素。</p>
<h4 id="16-html常见兼容性问题？"><a href="#16-html常见兼容性问题？" class="headerlink" title="16.html常见兼容性问题？"></a>16.html常见兼容性问题？</h4><ol>
<li>png24位的图片在iE6浏览器上出现背景</li>
</ol>
<p>解决方案：做成PNG8，也可以引用一段脚本处理.</p>
<ol start="2">
<li>浏览器默认的margin和padding不同</li>
</ol>
<p>解决方案：加一个全局的 *{margin:0;padding:0;} 来统一。</p>
<ol start="3">
<li>IE6双边距bug：在IE6下，如果对元素设置了浮动，同时又设置了margin-left或margin-right，margin值会加倍。</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#box&#123; float:left; width:10px; margin:0 0 0 10px;&#125;</span><br></pre></td></tr></table></figure>

<p>这种情况之下IE会产生20px的距离</p>
<p>解决方案：在float的标签样式控制中加入 _display:inline; 将其转化为行内属性。( _ 这个符号只有ie6会识别)</p>
<ol start="4">
<li>渐进识别的方式，从总体中逐渐排除局部。 </li>
</ol>
<p>首先，巧妙的使用“\9”这一标记，将IE游览器从所有情况中分离出来。 </p>
<p>接着，再次使用 “+” 将IE8和IE7、IE6分离开来，这样IE8已经独立识别。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.bb&#123;  </span><br><span class="line">background-color:#f1ee18; &#x2F;*所有识别*&#x2F;  </span><br><span class="line">.background-color:#00deff\9; &#x2F;*IE6、7、8识别*&#x2F; </span><br><span class="line">+background-color:#a200ff; &#x2F;*IE6、7识别*&#x2F;  </span><br><span class="line">_background-color:#1e0bd1; &#x2F;*IE6识别*&#x2F;&#125;</span><br></pre></td></tr></table></figure>

<p>5.IE下，可以使用获取常规属性的方法来获取自定义属性，也可以使用 getAttribute() 获取自定义属性；Firefox下,只能使用getAttribute()获取自定义属性</p>
<p>解决方法：统一通过getAttribute()获取自定义属性</p>
<ol start="6">
<li>IE下，event对象有 x、y 属性，但是没有 pageX、pageY属性; Firefox下，event对象有 pageX、pageY 属性，但是没有 x、y 属性</li>
</ol>
<p>解决方法：（条件注释）缺点是在IE浏览器下可能会增加额外的HTTP请求数。</p>
<ol start="7">
<li>Chrome 中文界面下默认会将小于 12px 的文本强制按照 12px 显示</li>
</ol>
<p>解决方法：可通过加入 CSS 属性 -webkit-text-size-adjust: none; 解决</p>
<ol start="8">
<li>超链接访问过后 hover 样式就不出现了，被点击访问过的超链接样式不在具有 hover 和 active 了</li>
</ol>
<p>解决方法：改变CSS属性的排列顺序 L-V-H-A</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">a:link &#123;&#125;</span><br><span class="line">a:visited &#123;&#125;</span><br><span class="line">a:hover &#123;&#125;</span><br><span class="line">a:active &#123;&#125;</span><br></pre></td></tr></table></figure>

<ol start="9">
<li><p>怪异模式问题：漏写 DTD 声明，Firefox 仍然会按照标准模式来解析网页，但在 IE 中会触发怪异模式。为避免怪异模式给我们带来不必要的麻烦，最好养成书写 DTD 声明的好习惯。现在可以使用<a href="http://www.w3.org/TR/html5/single-page.html" target="_blank" rel="noopener">html5</a> 推荐的写法：<!DOCTYPE html></p>
</li>
<li><p>上下margin重合问题：ie和ff都存在，相邻的两个div的margin-left和margin-right不会重合，但是margin-top和margin-bottom却会发生重合。</p>
</li>
</ol>
<p>解决方法：养成良好的代码编写习惯，同时采用margin-top或者同时采用margin-bottom。</p>
<ol start="11">
<li>ie6对png图片格式支持不好</li>
</ol>
<p>解决方案：引用一段脚本处理</p>
<h4 id="17-解释下浮动和它的工作原理？清除浮动的技巧"><a href="#17-解释下浮动和它的工作原理？清除浮动的技巧" class="headerlink" title="17.解释下浮动和它的工作原理？清除浮动的技巧"></a>17.解释下浮动和它的工作原理？清除浮动的技巧</h4><p>浮动元素脱离文档流，不占据空间。浮动元素碰到包含它的边框或者浮动元素的边框停留。</p>
<ol>
<li>使用空标签清除浮动。</li>
</ol>
<p>这种方法是在所有浮动标签后面添加一个空标签 定义css clear:both. 弊端就是增加了无意义标签。</p>
<ol start="2">
<li>使用overflow。</li>
</ol>
<p>给包含浮动元素的父标签添加css属性 overflow:auto; zoom:1; zoom:1用于兼容IE6。</p>
<ol start="3">
<li>使用after伪对象清除浮动。</li>
</ol>
<p>该方法只适用于非IE浏览器。具体写法可参照以下示例。使用中需注意以下几点。一、该方法中必须为需要清除浮动元素的伪对象中设置 height:0，否则该元素会比实际高出若干像素；</p>
<p>可以给父元素设置overflow：auto或者hidden</p>
<h4 id="18-浮动元素引起的问题和解决办法？"><a href="#18-浮动元素引起的问题和解决办法？" class="headerlink" title="18.浮动元素引起的问题和解决办法？"></a>18.浮动元素引起的问题和解决办法？</h4><p>浮动元素引起的问题：</p>
<ol>
<li><p>父元素的高度无法被撑开，影响与父元素同级的元素</p>
</li>
<li><p>与浮动元素同级的非浮动元素会跟随其后</p>
</li>
<li><p>若非第一个元素浮动，则该元素之前的元素也需要浮动，否则会影响页面显示的结构</p>
</li>
</ol>
<p>解决方法：</p>
<p>使用 CSS 中的 clear:both; 属性来清除元素的浮动可解决2、3问题，对于问题1，添加如下样式，给父元素添加clearfix样式：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.clearfix:after&#123;content: &quot;.&quot;;display: block;height: 0;clear: both;visibility: hidden;&#125;</span><br><span class="line"></span><br><span class="line">.clearfix&#123;display: inline-block;&#125; &#x2F;* for IE&#x2F;Mac *&#x2F;</span><br></pre></td></tr></table></figure>

<p>清除浮动的几种方法：</p>
<ol>
<li><p>额外标签法，<div style="clear:both;"></div>（缺点：不过这个办法会增加额外的标签使HTML结构看起来不够简洁。）</p>
</li>
<li><p>使用after伪元素</p>
</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#parent:after&#123;  </span><br><span class="line">   content:&quot;.&quot;;  </span><br><span class="line">   heightx:0;</span><br><span class="line">   visibility:hidden;  </span><br><span class="line">   display:block;  </span><br><span class="line">   clear:both;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li><p>浮动外部元素x</p>
</li>
<li><p>设置 overflow 为 hidden 或者 auto</p>
</li>
</ol>
<h4 id="19-IE-8以下版本的浏览器中的盒模型有什么不同"><a href="#19-IE-8以下版本的浏览器中的盒模型有什么不同" class="headerlink" title="19.IE 8以下版本的浏览器中的盒模型有什么不同"></a>19.IE 8以下版本的浏览器中的盒模型有什么不同</h4><p>IE8以下浏览器的盒模型中定义的元素的宽高包括内边距和边框</p>
<h4 id="20-DOM操作——怎样添加、移除、移动、复制、创建和查找节点。"><a href="#20-DOM操作——怎样添加、移除、移动、复制、创建和查找节点。" class="headerlink" title="20.DOM操作——怎样添加、移除、移动、复制、创建和查找节点。"></a>20.DOM操作——怎样添加、移除、移动、复制、创建和查找节点。</h4><ol>
<li>创建新节点</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">createDocumentFragment() &#x2F;&#x2F; 创建一个DOM片段</span><br><span class="line">createElement() &#x2F;&#x2F; 创建一个具体的元素</span><br><span class="line">createTextNode() &#x2F;&#x2F; 创建一个文本节点</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>添加、移除、替换、插入</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">appendChild()</span><br><span class="line">removeChild()</span><br><span class="line">replaceChild()</span><br><span class="line">insertBefore() &#x2F;&#x2F; 在已有的子节点前插入一个新的子节点</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>查找</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">getElementsByTagName() &#x2F;&#x2F; 通过标签名称</span><br><span class="line">getElementsByName()&#x2F;&#x2F; 通过元素的Name属性的值(IE容错能力较强，会得到一个数组，其中包括id等于name值的)</span><br><span class="line">getElementById() &#x2F;&#x2F; 通过元素Id，唯一性</span><br></pre></td></tr></table></figure>

<h4 id="21-html5有哪些新特性、移除了那些元素？如何处理HTML5新标签的浏览器兼容问题？如何区分-HTML-和-HTML5？"><a href="#21-html5有哪些新特性、移除了那些元素？如何处理HTML5新标签的浏览器兼容问题？如何区分-HTML-和-HTML5？" class="headerlink" title="21.html5有哪些新特性、移除了那些元素？如何处理HTML5新标签的浏览器兼容问题？如何区分 HTML 和 HTML5？"></a>21.html5有哪些新特性、移除了那些元素？如何处理HTML5新标签的浏览器兼容问题？如何区分 HTML 和 HTML5？</h4><p>新特性：</p>
<p>HTML5 现在已经不是 SGML 的子集，主要是关于图像，位置，存储，多任务等功能的增加。</p>
<ol>
<li><p>拖拽释放(Drag and drop) API </p>
</li>
<li><p>语义化更好的内容标签（header,nav,footer,aside,article,section）</p>
</li>
<li><p>音频、视频API(audio,video)</p>
</li>
<li><p>画布(Canvas) API</p>
</li>
<li><p>地理(Geolocation) API</p>
</li>
<li><p>本地离线存储 localStorage 长期存储数据，浏览器关闭后数据不丢失；</p>
</li>
<li><p>sessionStorage 的数据在浏览器关闭后自动删除</p>
</li>
<li><p>表单控件，calendar、date、time、email、url、search  </p>
</li>
<li><p>新的技术webworker, websocket, Geolocation</p>
</li>
</ol>
<p>移除的元素：</p>
<ol>
<li><p>纯表现的元素：basefont，big，center，font, s，strike，tt，u；</p>
</li>
<li><p>对可用性产生负面影响的元素：frame，frameset，noframes；</p>
</li>
</ol>
<p>支持HTML5新标签：</p>
<ol>
<li>IE8/IE7/IE6支持通过 document.createElement 方法产生的标签，可以利用这一特性让这些浏览器支持 HTML5 新标签，浏览器支持新标签后，还需要添加标签默认的样式（当然最好的方式是直接使用成熟的框架、使用最多的是html5shiv框架）：</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!--[if lt IE 9]&gt; </span><br><span class="line">&lt;script&gt; src&#x3D;&quot;http:&#x2F;&#x2F;html5shiv.googlecode.com&#x2F;svn&#x2F;trunk&#x2F;html5.js&lt;&#x2F;script&gt; &lt;![endif]--&gt;</span><br></pre></td></tr></table></figure>

<p>如何区分： </p>
<p>DOCTYPE声明新增的结构元素、功能元素</p>
<h4 id="22-iframe的优缺点？"><a href="#22-iframe的优缺点？" class="headerlink" title="22.iframe的优缺点？"></a>22.iframe的优缺点？</h4><p>优点：</p>
<ol>
<li><p>解决加载缓慢的第三方内容如图标和广告等的加载问题</p>
</li>
<li><p>Security sandbox</p>
</li>
<li><p>并行加载脚本</p>
</li>
</ol>
<p>缺点：</p>
<ol>
<li><p>iframe会阻塞主页面的Onload事件</p>
</li>
<li><p>即时内容为空，加载也需要时间</p>
</li>
<li><p>没有语意</p>
</li>
</ol>
<h4 id="23-如何实现浏览器内多个标签页之间的通信"><a href="#23-如何实现浏览器内多个标签页之间的通信" class="headerlink" title="23.如何实现浏览器内多个标签页之间的通信?"></a>23.如何实现浏览器内多个标签页之间的通信?</h4><p>调用 localstorge、cookies 等本地存储方式</p>
<h4 id="24-webSocket-如何兼容低浏览器？"><a href="#24-webSocket-如何兼容低浏览器？" class="headerlink" title="24.webSocket 如何兼容低浏览器？"></a>24.webSocket 如何兼容低浏览器？</h4><p>Adobe Flash Socket 、 ActiveX HTMLFile (IE) 、 基于 multipart 编码发送 XHR 、 基于长轮询的 XHR</p>
<h4 id="25-线程与进程的区别"><a href="#25-线程与进程的区别" class="headerlink" title="25.线程与进程的区别"></a>25.线程与进程的区别</h4><ol>
<li><p>一个程序至少有一个进程,一个进程至少有一个线程</p>
</li>
<li><p>线程的划分尺度小于进程，使得多线程程序的并发性高</p>
</li>
<li><p>另外，进程在执行过程中拥有独立的内存单元，而多个线程共享内存，从而极大地提高了程序的运行效率</p>
</li>
<li><p>线程在执行过程中与进程还是有区别的。每个独立的线程有一个程序运行的入口、顺序执行序列和程序的出口。但是线程不能够独立执行，必须依存在应用程序中，由应用程序提供多个线程执行控制 </p>
</li>
<li><p>从逻辑角度来看，多线程的意义在于一个应用程序中，有多个执行部分可以同时执行。但操作系统并没有将多个线程看做多个独立的应用，来实现进程的调度和管理以及资源分配。这就是进程和线程的重要区别</p>
</li>
</ol>
<h4 id="26-你如何对网站的文件和资源进行优化？"><a href="#26-你如何对网站的文件和资源进行优化？" class="headerlink" title="26.你如何对网站的文件和资源进行优化？"></a>26.你如何对网站的文件和资源进行优化？</h4><p>期待的解决方案包括：</p>
<ol>
<li><p>文件合并</p>
</li>
<li><p>文件最小化/文件压缩</p>
</li>
<li><p>使用 CDN 托管</p>
</li>
<li><p>缓存的使用（多个域名来提供缓存）</p>
</li>
<li><p>其他</p>
</li>
</ol>
<h4 id="27-请说出三种减少页面加载时间的方法"><a href="#27-请说出三种减少页面加载时间的方法" class="headerlink" title="27.请说出三种减少页面加载时间的方法"></a>27.请说出三种减少页面加载时间的方法</h4><ol>
<li><p>优化图片 </p>
</li>
<li><p>图像格式的选择（GIF：提供的颜色较少，可用在一些对颜色要求不高的地方） </p>
</li>
<li><p>优化CSS（压缩合并css，如 margin-top, margin-left…) </p>
</li>
<li><p>网址后加斜杠（如<a href="http://www.campr.com/目录，会判断这个目录是什么文件类型，或者是目录。）" target="_blank" rel="noopener">www.campr.com/目录，会判断这个目录是什么文件类型，或者是目录。）</a> </p>
</li>
<li><p>标明高度和宽度（如果浏览器没有找到这两个参数，它需要一边下载图片一边计算大小，如果图片很多，浏览器需要不断地调整页面。这不但影响速度，也影响浏览体验。 </p>
</li>
</ol>
<p>当浏览器知道了高度和宽度参数后，即使图片暂时无法显示，页面上也会腾出图片的空位，然后继续加载后面的内容。从而加载时间快了，浏览体验也更好了） </p>
<ol start="6">
<li>减少http请求（合并文件，合并图片）</li>
</ol>
<h4 id="28-你都使用哪些工具来测试代码的性能？"><a href="#28-你都使用哪些工具来测试代码的性能？" class="headerlink" title="28.你都使用哪些工具来测试代码的性能？"></a>28.你都使用哪些工具来测试代码的性能？</h4><ol>
<li><p>Profiler</p>
</li>
<li><p>JSPerf（<a href="http://jsperf.com/nexttick-vs-setzerotimeout-vs-settimeout）" target="_blank" rel="noopener">http://jsperf.com/nexttick-vs-setzerotimeout-vs-settimeout）</a></p>
</li>
<li><p>Dromaeo</p>
</li>
</ol>
<h4 id="29-什么是-FOUC（无样式内容闪烁）？你如何来避免-FOUC？"><a href="#29-什么是-FOUC（无样式内容闪烁）？你如何来避免-FOUC？" class="headerlink" title="29.什么是 FOUC（无样式内容闪烁）？你如何来避免 FOUC？"></a>29.什么是 FOUC（无样式内容闪烁）？你如何来避免 FOUC？</h4><p>OUC - Flash Of Unstyled Content 文档样式闪烁</p>
<style type="text/css" media="all">@import "../fouc.css";</style> 

<p>而引用CSS文件的@import就是造成这个问题的罪魁祸首。IE会先加载整个HTML文档的DOM，然后再去导入外部的CSS文件，因此，在页面DOM加载完成到CSS导入完成中间会有一段时间页面上的内容是没有样式的，这段时间的长短跟网速，电脑速度都有关系。</p>
<p>解决方法简单的出奇，只要在<head>之间加入一个<link>或者<script>元素就可以了。</p>
<h4 id="30-null和undefined的区别？"><a href="#30-null和undefined的区别？" class="headerlink" title="30.null和undefined的区别？"></a>30.null和undefined的区别？</h4><p>null是一个表示"无"的对象，转为数值时为0</p>
<p>undefined是一个表示"无"的原始值，转为数值时为NaN</p>
<p>当声明的变量还未被初始化时，变量的默认值为undefined</p>
<p>null用来表示尚未存在的对象，常用来表示函数企图返回一个不存在的对象</p>
<p>undefined表示 “缺少值”，就是此处应该有一个值，但是还没有定义。典型用法是：</p>
<ol>
<li><p>变量被声明了，但没有赋值时，就等于 undefined</p>
</li>
<li><p>调用函数时，应该提供的参数没有提供，该参数等于 undefined</p>
</li>
<li><p>对象没有赋值的属性，该属性的值为 undefined</p>
</li>
<li><p>函数没有返回值时，默认返回 undefined</p>
</li>
</ol>
<p>null表示“没有对象”，即该处不应该有值。典型用法是：</p>
<ol>
<li><p>作为函数的参数，表示该函数的参数不是对象</p>
</li>
<li><p>作为对象原型链的终点</p>
</li>
</ol>
<h4 id="31-new操作符具体干了什么呢"><a href="#31-new操作符具体干了什么呢" class="headerlink" title="31.new操作符具体干了什么呢?"></a>31.new操作符具体干了什么呢?</h4><ol>
<li><p>创建一个空对象，并且 this 变量引用该对象，同时还继承了该函数的原型</p>
</li>
<li><p>属性和方法被加入到 this 引用的对象中</p>
</li>
<li><p>新创建的对象由 this 所引用，并且最后隐式的返回 this</p>
</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">var obj  &#x3D; &#123;&#125;;</span><br><span class="line">obj.__proto__ &#x3D; Base.prototype;</span><br><span class="line">Base.call(obj);</span><br></pre></td></tr></table></figure>

<h4 id="32-对JSON-的了解？"><a href="#32-对JSON-的了解？" class="headerlink" title="32.对JSON 的了解？"></a>32.对JSON 的了解？</h4><p>JSON(JavaScript Object Notation) 是一种轻量级的数据交换格式。它是基于JavaScript的一个子集。数据格式简单, 易于读写, 占用带宽小。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;&quot;age&quot;:&quot;12&quot;, &quot;name&quot;:&quot;back&quot;&#125;</span><br></pre></td></tr></table></figure>

<h4 id="33-js延迟加载的方式有哪些？"><a href="#33-js延迟加载的方式有哪些？" class="headerlink" title="33.js延迟加载的方式有哪些？"></a>33.js延迟加载的方式有哪些？</h4><ol>
<li><p>defer和async</p>
</li>
<li><p>动态创建DOM方式（创建script，插入到DOM中，加载完毕后callBack）</p>
</li>
<li><p>按需异步载入js</p>
</li>
</ol>
<h4 id="34-如何解决跨域问题"><a href="#34-如何解决跨域问题" class="headerlink" title="34.如何解决跨域问题?"></a>34.如何解决跨域问题?</h4><ol>
<li><p>jsonp（jsonp 的原理是动态插入 script 标签）</p>
</li>
<li><p>document.domain + iframe</p>
</li>
<li><p>window.name、window.postMessage</p>
</li>
<li><p>服务器上设置代理页面</p>
</li>
</ol>
<h4 id="35-document-write和-innerHTML-的区别"><a href="#35-document-write和-innerHTML-的区别" class="headerlink" title="35.document.write和 innerHTML 的区别"></a>35.document.write和 innerHTML 的区别</h4><p>document.write 只能重绘整个页面</p>
<p>innerHTML 可以重绘页面的一部分</p>
<h4 id="36-call-和-apply-的作用？"><a href="#36-call-和-apply-的作用？" class="headerlink" title="36..call() 和 .apply() 的作用？"></a>36..call() 和 .apply() 的作用？</h4><p>动态改变某个类的某个方法的运行环境</p>
<h4 id="37-哪些操作会造成内存泄漏？"><a href="#37-哪些操作会造成内存泄漏？" class="headerlink" title="37.哪些操作会造成内存泄漏？"></a>37.哪些操作会造成内存泄漏？</h4><p>内存泄漏指任何对象在您不再拥有或需要它之后仍然存在。</p>
<p>垃圾回收器定期扫描对象，并计算引用了每个对象的其他对象的数量。如果一个对象的引用数量为 0（没有其他对象引用过该对象），或对该对象的惟一引用是循环的，那么该对象的内存即可回收。</p>
<ol>
<li><p>setTimeout 的第一个参数使用字符串而非函数的话，会引发内存泄漏。</p>
</li>
<li><p>闭包</p>
</li>
<li><p>控制台日志</p>
</li>
<li><p>循环（在两个对象彼此引用且彼此保留时，就会产生一个循环）</p>
</li>
</ol>
<h4 id="38-如何判断当前脚本运行在浏览器还是node环境中？"><a href="#38-如何判断当前脚本运行在浏览器还是node环境中？" class="headerlink" title="38.如何判断当前脚本运行在浏览器还是node环境中？"></a>38.如何判断当前脚本运行在浏览器还是node环境中？</h4><p>通过判断 Global 对象是否为window，如果不为window，当前脚本没有运行在浏览器中。即在node中的全局变量是global ,浏览器的全局变量是window。 可以通过该全局变量是否定义来判断宿主环境</p>
<h4 id="39-什么叫优雅降级和渐进增强？"><a href="#39-什么叫优雅降级和渐进增强？" class="headerlink" title="39.什么叫优雅降级和渐进增强？"></a>39.什么叫优雅降级和渐进增强？</h4><ol>
<li><p>优雅降级：Web站点在所有新式浏览器中都能正常工作，如果用户使用的是老式浏览器，则代码会检查以确认它们是否能正常工作。由于IE独特的盒模型布局问题，针对不同版本的IE的hack实践过优雅降级了,为那些无法支持功能的浏览器增加候选方案，使之在旧式浏览器上以某种形式降级体验却不至于完全失效。</p>
</li>
<li><p>渐进增强：从被所有浏览器支持的基本功能开始，逐步地添加那些只有新式浏览器才支持的功能,向页面增加无害于基础浏览器的额外样式和功能的。当浏览器支持时，它们会自动地呈现出来并发挥作用。</p>
</li>
</ol>
<h4 id="40-对Node的优点和缺点提出了自己的看法？"><a href="#40-对Node的优点和缺点提出了自己的看法？" class="headerlink" title="40.对Node的优点和缺点提出了自己的看法？"></a>40.对Node的优点和缺点提出了自己的看法？</h4><p>优点：</p>
<ol>
<li><p>因为Node是基于事件驱动和无阻塞的，所以非常适合处理并发请求，因此构建在Node上的代理服务器相比其他技术实现（如Ruby）的服务器表现要好得多。</p>
</li>
<li><p>与Node代理服务器交互的客户端代码是由javascript语言编写的，因此客户端和服务器端都用同一种语言编写，这是非常美妙的事情。</p>
</li>
</ol>
<p>缺点：</p>
<ol>
<li><p>Node是一个相对新的开源项目，所以不太稳定，它总是一直在变。</p>
</li>
<li><p>缺少足够多的第三方库支持。看起来，就像是Ruby/Rails当年的样子（第三方库现在已经很丰富了，所以这个缺点可以说不存在了）。</p>
</li>
</ol>
<h4 id="41-对前端界面工程师这个职位是怎么样理解的？它的前景会怎么样？"><a href="#41-对前端界面工程师这个职位是怎么样理解的？它的前景会怎么样？" class="headerlink" title="41.对前端界面工程师这个职位是怎么样理解的？它的前景会怎么样？"></a>41.对前端界面工程师这个职位是怎么样理解的？它的前景会怎么样？</h4><p>前端是最贴近用户的程序员，比后端、数据库、产品经理、运营、安全都近。</p>
<ol>
<li><p>实现界面交互</p>
</li>
<li><p>提升用户体验</p>
</li>
<li><p>有了Node.js，前端可以实现服务端的一些事情</p>
</li>
</ol>
<p>前景：</p>
<ol>
<li><p>前端是最贴近用户的程序员，前端的能力就是能让产品从 90分进化到 100 分，甚至更好</p>
</li>
<li><p>参与项目，快速高质量完成实现效果图，精确到1px；</p>
</li>
<li><p>与团队成员，UI设计，产品经理的沟通；</p>
</li>
<li><p>做好的页面结构，页面重构和用户体验；</p>
</li>
<li><p>处理hack，兼容、写出优美的代码格式；</p>
</li>
<li><p>针对服务器的优化、拥抱最新前端技术。</p>
</li>
</ol>
<h4 id="42-你有哪些性能优化的方法？"><a href="#42-你有哪些性能优化的方法？" class="headerlink" title="42.你有哪些性能优化的方法？"></a>42.你有哪些性能优化的方法？</h4><ol>
<li><p>减少http请求次数：CSS Sprites, JS、CSS 源码压缩、图片大小控制合适；网页 Gzip，CDN 托管，data 缓存 ，图片服务器</p>
</li>
<li><p>前端模板 JS + 数据，减少由于HTML标签导致的带宽浪费，前端用变量保存 AJAX 请求结果，每次操作本地变量，不用请求，减少请求次数</p>
</li>
<li><p>用 innerHTML 代替 DOM 操作，减少 DOM 操作次数，优化 javascript 性能</p>
</li>
<li><p>当需要设置的样式很多时设置 className 而不是直接操作 style</p>
</li>
<li><p>少用全局变量、缓存DOM节点查找的结果。减少 IO 读取操作</p>
</li>
<li><p>避免使用 CSS Expression（css表达式)又称 Dynamic properties(动态属性)</p>
</li>
<li><p>图片预加载，将样式表放在顶部，将脚本放在底部，加上时间戳</p>
</li>
</ol>
<h4 id="43-http状态码有那些？分别代表是什么意思？"><a href="#43-http状态码有那些？分别代表是什么意思？" class="headerlink" title="43.http状态码有那些？分别代表是什么意思？"></a>43.http状态码有那些？分别代表是什么意思？</h4><ol>
<li><p>100-199 用于指定客户端应响应的某些动作</p>
</li>
<li><p>200-299 用于表示请求成功</p>
</li>
<li><p>300-399 用于已经移动的文件并且常被包含在定位头信息中指定新的地址信息</p>
</li>
<li><p>400-499 用于指出客户端的错误</p>
</li>
</ol>
<p>​       400：语义有误，当前请求无法被服务器理解</p>
<p>​       401：当前请求需要用户验证</p>
<p>​       403：服务器已经理解请求，但是拒绝执行它</p>
<ol start="5">
<li>500-599 用于指出服务器错误</li>
</ol>
<p>​        503：服务不可用</p>
<h4 id="44-一个页面从输入-URL-到页面加载显示完成，这个过程中都发生了什么？"><a href="#44-一个页面从输入-URL-到页面加载显示完成，这个过程中都发生了什么？" class="headerlink" title="44.一个页面从输入 URL 到页面加载显示完成，这个过程中都发生了什么？"></a>44.一个页面从输入 URL 到页面加载显示完成，这个过程中都发生了什么？</h4><p>分为4个步骤：</p>
<ol>
<li><p>当发送一个 URL 请求时，不管这个 URL 是 Web 页面的 URL 还是 Web 页面上每个资源的 URL，浏览器都会开启一个线程来处理这个请求，同时在远程 DNS 服务器上启动一个 DNS 查询。这能使浏览器获得请求对应的 IP 地址。</p>
</li>
<li><p>浏览器与远程 Web 服务器通过 TCP 三次握手协商来建立一个 TCP/IP 连接。该握手包括一个同步报文，一个同步-应答报文和一个应答报文，这三个报文在 浏览器和服务器之间传递。该握手首先由客户端尝试建立起通信，而后服务器应答并接受客户端的请求，最后由客户端发出该请求已经被接受的报文。</p>
</li>
<li><p>一旦 TCP/IP 连接建立，浏览器会通过该连接向远程服务器发送 HTTP 的 GET 请求。远程服务器找到资源并使用 HTTP 响应返回该资源，值为 200 的 HTTP 响应状态表示一个正确的响应。</p>
</li>
<li><p>此时，Web 服务器提供资源服务，客户端开始下载资源。</p>
</li>
</ol>
<p>请求返回后，便进入了我们关注的前端模块</p>
<p>简单来说，浏览器会解析 HTML 生成 DOM Tree，其次会根据 CSS 生成 CSS Rule Tree，而 javascript 又可以根据 DOM API 操作 DOM</p>
<h4 id="45-平时如何管理你的项目？"><a href="#45-平时如何管理你的项目？" class="headerlink" title="45.平时如何管理你的项目？"></a>45.平时如何管理你的项目？</h4><p>   1.先期团队必须确定好全局样式（globe.css），编码模式(utf-8) 等</p>
<ol start="2">
<li><p>编写习惯必须一致（例如都是采用继承式的写法，单样式都写成一行）</p>
</li>
<li><p>标注样式编写人，各模块都及时标注（标注关键样式调用的地方）</p>
</li>
<li><p>页面进行标注（例如 页面 模块 开始和结束）</p>
</li>
<li><p>CSS 跟 HTML 分文件夹并行存放，命名都得统一（例如 style.css）</p>
</li>
<li><p>JS 分文件夹存放 命名以该 JS 功能为准的英文翻译</p>
</li>
<li><p>图片采用整合的 images.png png8 格式文件使用 尽量整合在一起使用方便将来的管理 </p>
</li>
</ol>
<h4 id="46-javascript对象的几种创建方式"><a href="#46-javascript对象的几种创建方式" class="headerlink" title="46.javascript对象的几种创建方式"></a>46.javascript对象的几种创建方式</h4><ol>
<li><p>工厂模式</p>
</li>
<li><p>构造函数模式</p>
</li>
<li><p>原型模式</p>
</li>
<li><p>混合构造函数和原型模式</p>
</li>
<li><p>动态原型模式</p>
</li>
<li><p>寄生构造函数模式</p>
</li>
<li><p>稳妥构造函数模式</p>
</li>
</ol>
<h4 id="47-javascript继承的-6-种方法"><a href="#47-javascript继承的-6-种方法" class="headerlink" title="47.javascript继承的 6 种方法"></a>47.javascript继承的 6 种方法</h4><ol>
<li><p>原型链继承</p>
</li>
<li><p>借用构造函数继承</p>
</li>
<li><p>组合继承(原型+借用构造)</p>
</li>
<li><p>原型式继承</p>
</li>
<li><p>寄生式继承</p>
</li>
<li><p>寄生组合式继承</p>
</li>
</ol>
<h4 id="48-ajax-的过程是怎样的"><a href="#48-ajax-的过程是怎样的" class="headerlink" title="48.ajax 的过程是怎样的"></a>48.ajax 的过程是怎样的</h4><ol>
<li><p>创建XMLHttpRequest对象,也就是创建一个异步调用对象</p>
</li>
<li><p>创建一个新的HTTP请求,并指定该HTTP请求的方法、URL及验证信息</p>
</li>
<li><p>设置响应HTTP请求状态变化的函数</p>
</li>
<li><p>发送HTTP请求</p>
</li>
<li><p>获取异步调用返回的数据</p>
</li>
<li><p>使用JavaScript和DOM实现局部刷新</p>
</li>
</ol>
<h4 id="49-异步加载和延迟加载"><a href="#49-异步加载和延迟加载" class="headerlink" title="49.异步加载和延迟加载"></a>49.异步加载和延迟加载</h4><ol>
<li><p>异步加载的方案： 动态插入 script 标签</p>
</li>
<li><p>通过 ajax 去获取 js 代码，然后通过 eval 执行</p>
</li>
<li><p>script 标签上添加 defer 或者 async 属性</p>
</li>
<li><p>创建并插入 iframe，让它异步执行 js</p>
</li>
<li><p>延迟加载：有些 js 代码并不是页面初始化的时候就立刻需要的，而稍后的某些情况才需要的</p>
</li>
</ol>
<h4 id="50-前端的安全问题？"><a href="#50-前端的安全问题？" class="headerlink" title="50.前端的安全问题？"></a>50.前端的安全问题？</h4><ol>
<li><p>XSS</p>
</li>
<li><p>sql注入</p>
</li>
<li><p>CSRF：是跨站请求伪造，很明显根据刚刚的解释，他的核心也就是请求伪造，通过伪造身份提交POST和GET请求来进行跨域的攻击</p>
</li>
</ol>
<p>完成CSRF需要两个步骤：</p>
<ol>
<li><p>登陆受信任的网站A，在本地生成 COOKIE</p>
</li>
<li><p>在不登出A的情况下，或者本地 COOKIE 没有过期的情况下，访问危险网站B。</p>
</li>
</ol>
<h4 id="51-ie-各版本和-chrome-可以并行下载多少个资源"><a href="#51-ie-各版本和-chrome-可以并行下载多少个资源" class="headerlink" title="51.ie 各版本和 chrome 可以并行下载多少个资源"></a>51.ie 各版本和 chrome 可以并行下载多少个资源</h4><ol>
<li><p>IE6 2 个并发</p>
</li>
<li><p>iE7 升级之后的 6 个并发，之后版本也是 6 个</p>
</li>
<li><p>Firefox，chrome 也是6个</p>
</li>
</ol>
<h4 id="52-javascript里面的继承怎么实现，如何避免原型链上面的对象共享"><a href="#52-javascript里面的继承怎么实现，如何避免原型链上面的对象共享" class="headerlink" title="52.javascript里面的继承怎么实现，如何避免原型链上面的对象共享"></a>52.javascript里面的继承怎么实现，如何避免原型链上面的对象共享</h4><p>用构造函数和原型链的混合模式去实现继承，避免对象共享可以参考经典的extend()函数，很多前端框架都有封装的，就是用一个空函数当做中间变量</p>
<h4 id="53-grunt，-YUI-compressor-和-google-clojure用来进行代码压缩的用法。"><a href="#53-grunt，-YUI-compressor-和-google-clojure用来进行代码压缩的用法。" class="headerlink" title="53.grunt， YUI compressor 和 google clojure用来进行代码压缩的用法。"></a>53.grunt， YUI compressor 和 google clojure用来进行代码压缩的用法。</h4><p>grunt：</p>
<p>UglifyJS 是基于 NodeJS 的 Javascript 语法解析/压缩/格式化工具</p>
<p>官网：<a href="http://lisperator.net/uglifyjs/" target="_blank" rel="noopener">http://lisperator.net/uglifyjs/</a> 或者 <a href="https://github.com/mishoo/UglifyJS2">https://github.com/mishoo/UglifyJS2</a></p>
<p>安装：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ npm install uglify-js -g</span><br></pre></td></tr></table></figure>

<p>使用方法见官网 demo</p>
<p>YUI compressor：</p>
<p>YUI Compressor 是一个用来压缩 JS 和 CSS 文件的工具，采用Java开发。</p>
<p>使用方法：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; 压缩JSx</span><br><span class="line">java -jar yuicompressor-2.4.2.jar --type js --charset utf-8 -v src.js &gt; packed.js</span><br><span class="line">&#x2F;&#x2F; 压缩CSS</span><br><span class="line">java -jar yuicompressor-2.4.2.jar --type&#96; &#96;css --charset utf-8 -v src.css &gt; packed.css</span><br></pre></td></tr></table></figure>

<p>Google Closure Compiler：</p>
<p>官网：<a href="https://developers.google.com/closure/compiler/" target="_blank" rel="noopener">https://developers.google.com/closure/compiler/</a></p>
<p>使用方法：</p>
<ol>
<li><p>在命令行下使用一个google编译好的java程序</p>
</li>
<li><p>使用google提供的在线服务</p>
</li>
<li><p>使用google提供的RESTful API</p>
</li>
</ol>
<h4 id="54-Flash、Ajax各自的优缺点，在使用中如何取舍？"><a href="#54-Flash、Ajax各自的优缺点，在使用中如何取舍？" class="headerlink" title="54.Flash、Ajax各自的优缺点，在使用中如何取舍？"></a>54.Flash、Ajax各自的优缺点，在使用中如何取舍？</h4><p>Flash：</p>
<ol>
<li><p>Flash适合处理多媒体、矢量图形、访问机器</p>
</li>
<li><p>对CSS、处理文本上不足，不容易被搜索</p>
</li>
</ol>
<p>Ajax：</p>
<ol>
<li><p>Ajax对CSS、文本支持很好，支持搜索</p>
</li>
<li><p>多媒体、矢量图形、机器访问不足</p>
</li>
</ol>
<p>共同点：</p>
<ol>
<li><p>与服务器的无刷新传递消息</p>
</li>
<li><p>可以检测用户离线和在线状态</p>
</li>
<li><p>操作DOM</p>
</li>
</ol>
<h4 id="55-请解释一下-JavaScript-的同源策略。"><a href="#55-请解释一下-JavaScript-的同源策略。" class="headerlink" title="55.请解释一下 JavaScript 的同源策略。"></a>55.请解释一下 JavaScript 的同源策略。</h4><p>概念：</p>
<p>同源策略是客户端脚本（尤其是Javascript）的重要的安全度量标准。它最早出自Netscape Navigator2.0，其目的是防止某个文档或脚本从多个不同源装载。</p>
<p>这里的同源策略指的是：协议，域名，端口相同，同源策略是一种安全协议，指一段脚本只能读取来自同一来源的窗口和文档的属性。</p>
<h4 id="56-为什么要有同源限制："><a href="#56-为什么要有同源限制：" class="headerlink" title="56.为什么要有同源限制："></a>56.为什么要有同源限制：</h4><p>我们举例说明：比如一个黑客程序，他利用Iframe把真正的银行登录页面嵌到他的页面上，当你使用真实的用户名，密码登录时，他的页面就可以通过Javascript读取到你的表单中input中的内容，这样用户名，密码就轻松到手了。</p>
<h4 id="57-什么是-use-strict-使用它的好处和坏处分别是什么？"><a href="#57-什么是-use-strict-使用它的好处和坏处分别是什么？" class="headerlink" title="57.什么是 "use strict"; ? 使用它的好处和坏处分别是什么？"></a>57.什么是 "use strict"; ? 使用它的好处和坏处分别是什么？</h4><p>ECMAscript 5添加了第二种运行模式："严格模式"（strict mode）。顾名思义，这种模式使得Javascript在更严格的条件下运行。</p>
<p>设立"严格模式"的目的，主要有以下几个：</p>
<ol>
<li><p>消除Javascript语法的一些不合理、不严谨之处，减少一些怪异行为;</p>
</li>
<li><p>消除代码运行的一些不安全之处，保证代码运行的安全；</p>
</li>
<li><p>提高编译器效率，增加运行速度；</p>
</li>
<li><p>为未来新版本的Javascript做好铺垫。</p>
</li>
</ol>
<p>注：经过测试 IE6,7,8,9 均不支持严格模式。</p>
<p>缺点：</p>
<p>现在网站的 JS 都会进行压缩，一些文件用了严格模式，而另一些没有。这时这些本来是严格模式的文件，被 merge 后，这个串就到了文件的中间，不仅没有指示严格模式，反而在压缩后浪费了字节。</p>
<h4 id="58-GET和POST的区别，何时使用POST？"><a href="#58-GET和POST的区别，何时使用POST？" class="headerlink" title="58.GET和POST的区别，何时使用POST？"></a>58.GET和POST的区别，何时使用POST？</h4><p>GET：一般用于信息获取，使用URL传递参数，对所发送信息的数量也有限制，一般在2000个字符</p>
<p>POST：一般用于修改服务器上的资源，对所发送的信息没有限制</p>
<p>GET方式需要使用 Request.QueryString 来取得变量的值</p>
<p>POST方式通过 Request.Form 来获取变量的值</p>
<p>也就是说 Get 是通过地址栏来传值，而 Post 是通过提交表单来传值。</p>
<p>在以下情况中，请使用 POST 请求：</p>
<ol>
<li><p>无法使用缓存文件（更新服务器上的文件或数据库）</p>
</li>
<li><p>向服务器发送大量数据（POST 没有数据量限制）</p>
</li>
<li><p>发送包含未知字符的用户输入时，POST 比 GET 更稳定也更可靠</p>
</li>
</ol>
<h4 id="59-哪些地方会出现css阻塞，哪些地方会出现js阻塞？"><a href="#59-哪些地方会出现css阻塞，哪些地方会出现js阻塞？" class="headerlink" title="59.哪些地方会出现css阻塞，哪些地方会出现js阻塞？"></a>59.哪些地方会出现css阻塞，哪些地方会出现js阻塞？</h4><p>js 的阻塞特性：所有浏览器在下载 JS 的时候，会阻止一切其他活动，比如其他资源的下载，内容的呈现等等。直到 JS 下载、解析、执行完毕后才开始继续并行下载其他资源并呈现内容。为了提高用户体验，新一代浏览器都支持并行下载 JS，但是 JS 下载仍然会阻塞其它资源的下载（例如.图片，css文件等）。</p>
<p>由于浏览器为了防止出现 JS 修改 DOM 树，需要重新构建 DOM 树的情况，所以就会阻塞其他的下载和呈现。</p>
<p>嵌入 JS 会阻塞所有内容的呈现，而外部 JS 只会阻塞其后内容的显示，2 种方式都会阻塞其后资源的下载。也就是说外部样式不会阻塞外部脚本的加载，但会阻塞外部脚本的执行。</p>
<p>CSS 怎么会阻塞加载了？CSS 本来是可以并行下载的，在什么情况下会出现阻塞加载了(在测试观察中，IE6 下 CSS 都是阻塞加载）</p>
<p>当 CSS 后面跟着嵌入的 JS 的时候，该 CSS 就会出现阻塞后面资源下载的情况。而当把嵌入 JS 放到 CSS 前面，就不会出现阻塞的情况了。</p>
<p>根本原因：因为浏览器会维持 html 中 css 和 js 的顺序，样式表必须在嵌入的 JS 执行前先加载、解析完。而嵌入的 JS 会阻塞后面的资源加载，所以就会出现上面 CSS 阻塞下载的情况。</p>
<p>嵌入JS应该放在什么位置？</p>
<ol>
<li><p>放在底部，虽然放在底部照样会阻塞所有呈现，但不会阻塞资源下载。</p>
</li>
<li><p>如果嵌入JS放在head中，请把嵌入JS放在CSS头部。</p>
</li>
<li><p>使用 defer（只支持IE）</p>
</li>
<li><p>不要在嵌入的JS中调用运行时间较长的函数，如果一定要用，可以用 setTimeout 来调用</p>
</li>
</ol>
<p>Javascript无阻塞加载具体方式：</p>
<ol>
<li><p>将脚本放在底部。<link>还是放在head中，用以保证在js加载前，能加载出正常显示的页面。<script>标签放在</body>前。</p>
</li>
<li><p>阻塞脚本：由于每个<script>标签下载时阻塞页面解析过程，所以限制页面的<script>总数也可以改善性能。适用于内联脚本和外部脚本。</p>
</li>
<li><p>非阻塞脚本：等页面完成加载后，再加载js代码。也就是，在 window.onload 事件发出后开始下载代码。</p>
</li>
<li><p>defer属性：支持IE4和fierfox3.5更高版本浏览器</p>
</li>
<li><p>动态脚本元素：文档对象模型（DOM）允许你使用js动态创建HTML的几乎全部文档内容。代码如下：</p>
</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;script&gt;  </span><br><span class="line">   var script&#x3D;document.createElement(&quot;script&quot;);  </span><br><span class="line">   script.type&#x3D;&quot;text&#x2F;javascript&quot;;  </span><br><span class="line">   script.src&#x3D;&quot;file.js&quot;; </span><br><span class="line">   document.getElementsByTagName(&quot;head&quot;)[0].appendChild(script);</span><br><span class="line">&lt;&#x2F;script&gt;</span><br></pre></td></tr></table></figure>

<p>此技术的重点在于：无论在何处启动下载，文件额下载和运行都不会阻塞其他页面处理过程，即使在head里（除了用于下载文件的 http 链接）。</p>
<h4 id="60-eval是做什么的？"><a href="#60-eval是做什么的？" class="headerlink" title="60.eval是做什么的？"></a>60.eval是做什么的？</h4><ol>
<li><p>它的功能是把对应的字符串解析成JS代码并运行</p>
</li>
<li><p>应该避免使用eval，不安全，非常耗性能（2次，一次解析成js语句，一次执行）</p>
</li>
</ol>
<h4 id="61-写一个通用的事件侦听器函数"><a href="#61-写一个通用的事件侦听器函数" class="headerlink" title="61.写一个通用的事件侦听器函数"></a>61.写一个通用的事件侦听器函数</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; event(事件)工具集，来源：github.com&#x2F;markyun</span><br><span class="line">markyun.Event &#x3D; &#123;</span><br><span class="line">  &#x2F;&#x2F; 页面加载完成后</span><br><span class="line">  readyEvent : function(fn) &#123;</span><br><span class="line">    if (fn&#x3D;&#x3D;null) &#123;</span><br><span class="line">      fn&#x3D;document;</span><br><span class="line">    &#125;</span><br><span class="line">    var oldonload &#x3D; window.onload;</span><br><span class="line">    if (typeof window.onload !&#x3D; &#39;function&#39;) &#123;</span><br><span class="line">      window.onload &#x3D; fn;</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">      window.onload &#x3D; function() &#123;</span><br><span class="line">        oldonload();</span><br><span class="line">        fn();</span><br><span class="line">      &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  &#x2F;&#x2F; 视能力分别使用dom0||dom2||IE方式 来绑定事件</span><br><span class="line">  &#x2F;&#x2F; 参数： 操作的元素,事件名称 ,事件处理程序</span><br><span class="line">  addEvent : function(element, type, handler) &#123;</span><br><span class="line">    if (element.addEventListener) &#123;</span><br><span class="line">      &#x2F;&#x2F;事件类型、需要执行的函数、是否捕捉</span><br><span class="line">      element.addEventListener(type, handler, false);</span><br><span class="line">    &#125; else if (element.attachEvent) &#123;</span><br><span class="line">      element.attachEvent(&#39;on&#39; + type, function() &#123;</span><br><span class="line">        handler.call(element);</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">      element[&#39;on&#39; + type] &#x3D; handler;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;, </span><br><span class="line">  &#x2F;&#x2F; 移除事件</span><br><span class="line">  removeEvent : function (element, type, handler) &#123;</span><br><span class="line">     if   (element.removeEnentListener) &#123;</span><br><span class="line">       element.removeEnentListener(type, handler,  false );</span><br><span class="line">     &#125;  else if (element.detachEvent) &#123;</span><br><span class="line">       element.detachEvent(&#39;on&#39; + type, handler);</span><br><span class="line">     &#125;  else   &#123;</span><br><span class="line">       element[ &#39;on&#39; + type] &#x3D; null;</span><br><span class="line">    &#125;</span><br><span class="line">   &#125;, </span><br><span class="line">   &#x2F;&#x2F; 阻止事件 (主要是事件冒泡，因为IE不支持事件捕获)</span><br><span class="line">  stopPropagation : function(ev) &#123;</span><br><span class="line">    if (ev.stopPropagation) &#123;</span><br><span class="line">      ev.stopPropagation();</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">      ev.cancelBubble &#x3D; true;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  &#x2F;&#x2F; 取消事件的默认行为</span><br><span class="line">  preventDefault : function(event) &#123;</span><br><span class="line">    if (event.preventDefault) &#123;</span><br><span class="line">      event.preventDefault();</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">      event.returnValue &#x3D; false;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  &#x2F;&#x2F; 获取事件目标</span><br><span class="line">  getTarget : function(event) &#123;</span><br><span class="line">    return event.target || event.srcElement;</span><br><span class="line">  &#125;,</span><br><span class="line">  &#x2F;&#x2F; 获取event对象的引用，取到事件的所有信息，确保随时能使用event；</span><br><span class="line">  getEvent : function(e) &#123;</span><br><span class="line">    var ev &#x3D; e || window.event;</span><br><span class="line">    if (!ev) &#123;</span><br><span class="line">      var c &#x3D; this.getEvent.caller;</span><br><span class="line">      while (c) &#123;</span><br><span class="line">        ev &#x3D; c.arguments[0];</span><br><span class="line">        if (ev &amp;&amp; Event &#x3D;&#x3D; ev.constructor) &#123;</span><br><span class="line">          break;</span><br><span class="line">        &#125;</span><br><span class="line">        c &#x3D; c.caller;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return ev;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h4 id="62-Node-js-的适用场景"><a href="#62-Node-js-的适用场景" class="headerlink" title="62.Node.js 的适用场景"></a>62.Node.js 的适用场景</h4><ol>
<li><p>高并发</p>
</li>
<li><p>聊天</p>
</li>
<li><p>实时消息推送</p>
</li>
</ol>
<h4 id="63-JavaScript-原型，原型链-有什么特点？"><a href="#63-JavaScript-原型，原型链-有什么特点？" class="headerlink" title="63.JavaScript 原型，原型链 ? 有什么特点？"></a>63.JavaScript 原型，原型链 ? 有什么特点？</h4><ol>
<li><p>原型对象也是普通的对象，是对象一个自带隐式的 <strong>proto</strong> 属性，原型也有可能有自己的原型，如果一个原型对象的原型不为 null 的话，我们就称之为原型链</p>
</li>
<li><p>原型链是由一些用来继承和共享属性的对象组成的（有限的）对象链</p>
</li>
</ol>
<h4 id="64-怎么重构页面？"><a href="#64-怎么重构页面？" class="headerlink" title="64.怎么重构页面？"></a>64.怎么重构页面？</h4><ol>
<li><p>编写 CSS</p>
</li>
<li><p>让页面结构更合理化，提升用户体验</p>
</li>
<li><p>实现良好的页面效果和提升性能</p>
</li>
</ol>
<h4 id="65-WEB应用从服务器主动推送Data到客户端有那些方式？"><a href="#65-WEB应用从服务器主动推送Data到客户端有那些方式？" class="headerlink" title="65.WEB应用从服务器主动推送Data到客户端有那些方式？"></a>65.WEB应用从服务器主动推送Data到客户端有那些方式？</h4><ol>
<li><p>html5 websocket</p>
</li>
<li><p>WebSocket 通过 Flash</p>
</li>
<li><p>XHR长时间连接</p>
</li>
<li><p>XHR Multipart Streaming</p>
</li>
<li><p>不可见的Iframe</p>
</li>
<li><script>标签的长时间连接(可跨域)

</li>
</ol>
<h4 id="66-事件、IE与火狐的事件机制有什么区别？-如何阻止冒泡？"><a href="#66-事件、IE与火狐的事件机制有什么区别？-如何阻止冒泡？" class="headerlink" title="66.事件、IE与火狐的事件机制有什么区别？ 如何阻止冒泡？"></a>66.事件、IE与火狐的事件机制有什么区别？ 如何阻止冒泡？</h4><ol>
<li><p>我们在网页中的某个操作（有的操作对应多个事件）。例如：当我们点击一个按钮就会产生一个事件。是可以被 JavaScript 侦测到的行为</p>
</li>
<li><p>事件处理机制：IE是事件冒泡、firefox同时支持两种事件模型，也就是：捕获型事件和冒泡型事件</p>
</li>
<li><p>ev.stopPropagation();</p>
</li>
</ol>
<p>注意旧ie的方法：ev.cancelBubble = true;</p>
<h4 id="67-Ajax-是什么？Ajax-的交互模型？同步和异步的区别？如何解决跨域问题？"><a href="#67-Ajax-是什么？Ajax-的交互模型？同步和异步的区别？如何解决跨域问题？" class="headerlink" title="67.Ajax 是什么？Ajax 的交互模型？同步和异步的区别？如何解决跨域问题？"></a>67.Ajax 是什么？Ajax 的交互模型？同步和异步的区别？如何解决跨域问题？</h4><p>Ajax 是什么：</p>
<ol>
<li><p>通过异步模式，提升了用户体验</p>
</li>
<li><p>优化了浏览器和服务器之间的传输，减少不必要的数据往返，减少了带宽占用</p>
</li>
<li><p>Ajax 在客户端运行，承担了一部分本来由服务器承担的工作，减少了大用户量下的服务器负载。</p>
</li>
</ol>
<p>Ajax 的最大的特点：</p>
<ol>
<li><p>Ajax可以实现动态不刷新（局部刷新）</p>
</li>
<li><p>readyState 属性 状态 有5个可取值： 0 = 未初始化，1 = 启动， 2 = 发送，3 = 接收，4 = 完成</p>
</li>
</ol>
<p>Ajax 同步和异步的区别:</p>
<ol>
<li><p>同步：提交请求 -> 等待服务器处理 -> 处理完毕返回，这个期间客户端浏览器不能干任何事</p>
</li>
<li><p>异步：请求通过事件触发 -> 服务器处理（这是浏览器仍然可以作其他事情）-> 处理完毕</p>
</li>
</ol>
<p>ajax.open方法中，第3个参数是设同步或者异步。</p>
<p>Ajax 的缺点：</p>
<ol>
<li><p>Ajax 不支持浏览器 back 按钮</p>
</li>
<li><p>安全问题 Ajax 暴露了与服务器交互的细节</p>
</li>
<li><p>对搜索引擎的支持比较弱</p>
</li>
<li><p>破坏了程序的异常机制</p>
</li>
<li><p>不容易调试</p>
</li>
</ol>
<p>解决跨域问题：</p>
<ol>
<li><p>jsonp</p>
</li>
<li><p>iframe</p>
</li>
<li><p>window.name、window.postMessage</p>
</li>
<li><p>服务器上设置代理页面</p>
</li>
</ol>
<h4 id="68-js对象的深度克隆代码实现"><a href="#68-js对象的深度克隆代码实现" class="headerlink" title="68.js对象的深度克隆代码实现"></a>68.js对象的深度克隆代码实现</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function clone(Obj) &#123;</span><br><span class="line">  var buf;  </span><br><span class="line">  if (Obj instanceof Array) &#123;</span><br><span class="line">    buf &#x3D; [];  &#x2F;&#x2F; 创建一个空的数组</span><br><span class="line">    var i &#x3D; Obj.length;</span><br><span class="line">    while (i--) &#123;</span><br><span class="line">      buf[i] &#x3D; clone(Obj[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    return buf;</span><br><span class="line">  &#125; else if (Obj xinstanceof Object)&#123;</span><br><span class="line">    buf &#x3D; &#123;&#125;;  x&#x2F;&#x2F; 创建一个空对象</span><br><span class="line">    for (var k in Obj) &#123;  &#x2F;&#x2F; 为这个对象添加新的属性</span><br><span class="line">      buf[k] &#x3D; clone(Obj[k]);</span><br><span class="line">    &#125;</span><br><span class="line">    return buf;</span><br><span class="line">  &#125;else&#123;</span><br><span class="line">    return Obj;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="69-对网站重构的理解"><a href="#69-对网站重构的理解" class="headerlink" title="69.对网站重构的理解"></a>69.对网站重构的理解</h4><p>网站重构：在不改变外部行为的前提下，简化结构、添加可读性，而在网站前端保持一致的行为。也就是说是在不改变 UI 的情况下，对网站进行优化，在扩展的同时保持一致的 UI。</p>
<p>对于传统的网站来说重构通常是：</p>
<ol>
<li><p>表格(table)布局改为 DIV + CSS</p>
</li>
<li><p>使网站前端兼容于现代浏览器(针对于不合规范的CSS、如对 IE6 有效的)</p>
</li>
<li><p>对于移动平台的优化</p>
</li>
<li><p>针对于 SEO 进行优化</p>
</li>
<li><p>深层次的网站重构应该考虑的方面</p>
</li>
<li><p>减少代码间的耦合</p>
</li>
<li><p>让代码保持弹性</p>
</li>
<li><p>严格按规范编写代码</p>
</li>
<li><p>设计可扩展的API</p>
</li>
<li><p>代替旧有的框架、语言(如VB)</p>
</li>
<li><p>增强用户体验</p>
</li>
<li><p>通常来说对于速度的优化也包含在重构中</p>
</li>
<li><p>压缩JS、CSS、image等前端资源(通常是由服务器来解决)</p>
</li>
<li><p>程序的性能优化(如数据读写)</p>
</li>
<li><p>采用CDN来加速资源加载</p>
</li>
<li><p>对于JS DOM的优化</p>
</li>
<li><p>HTTP服务器的文件缓存</p>
</li>
</ol>
<h4 id="70-如何获取UA"><a href="#70-如何获取UA" class="headerlink" title="70.如何获取UA"></a>70.如何获取UA</h4><script> 
function whatBrowser() {  
    document.Browser.Name.value=navigator.appName;  
    document.Browser.Version.value=navigator.appVersion;  
    document.Browser.Code.value=navigator.appCodeName;  
    document.Browser.Agent.value=navigator.userAgent;  
}
</script>

<h4 id="71-HTTP状态码"><a href="#71-HTTP状态码" class="headerlink" title="71.HTTP状态码"></a>71.HTTP状态码</h4><p>100 Continue  继续，一般在发送post请求时，已发送了http header之后服务端将返回此信息，表示确认，之后发送具体参数信息</p>
<p>200 OK  正常返回信息</p>
<p>201 Created  请求成功并且服务器创建了新的资源</p>
<p>202 Accepted  服务器已接受请求，但尚未处理</p>
<p>301 Moved Permanently  请求的网页已永久移动到新位置</p>
<p>302 Found  临时性重定向</p>
<p>303 See Other  临时性重定向，且总是使用 GET 请求新的 URI</p>
<p>304 Not Modified  自从上次请求后，请求的网页未修改过</p>
<p>400 Bad Request  服务器无法理解请求的格式，客户端不应当尝试再次使用相同的内容发起请求</p>
<p>401 Unauthorized  请求未授权</p>
<p>403 Forbidden  禁止访问</p>
<p>404 Not Found  找不到如何与 URI 相匹配的资源</p>
<p>500 Internal Server Error  最常见的服务器端错误</p>
<p>503 Service Unavailable 服务器端暂时无法处理请求（可能是过载或维护）</p>
<h4 id="72-cache-control"><a href="#72-cache-control" class="headerlink" title="72.cache-control"></a>72.cache-control</h4><p>网页的缓存是由HTTP消息头中的“Cache-control”来控制的，常见的取值有private、no-***、max-age、must-revalidate等，默认为private。</p>
<p>Expires 头部字段提供一个日期和时间，响应在该日期和时间后被认为失效。允许客户端在这个时间之前不去检查（发请求），等同max-age的效果。但是如果同时存在，则被Cache-Control的max-age覆盖。</p>
<p>Expires = "Expires" ":" HTTP-date</p>
<p>例如：</p>
<p>Expires: Thu, 01 Dec 1994 16:00:00 GMT （必须是GMT格式）</p>
<p>如果把它设置为-1，则表示立即过期</p>
<p>Expires 和 max-age 都可以用来指定文档的过期时间，但是二者有一些细微差别</p>
<ol>
<li><p>Expires在HTTP/1.0中已经定义，Cache-Control:max-age在HTTP/1.1中才有定义，为了向下兼容，仅使用max-age不够</p>
</li>
<li><p>Expires指定一个绝对的过期时间(GMT格式),这么做会导致至少2个问题：</p>
</li>
</ol>
<p>​       2.1客户端和服务器时间不同步导致Expires的配置出现问题。 </p>
<p>​       2.2很容易在配置后忘记具体的过期时间，导致过期来临出现浪涌现象</p>
<ol start="3">
<li><p>max-age 指定的是从文档被访问后的存活时间，这个时间是个相对值(比如:3600s)，相对的是文档第一次被请求时服务器记录的Request_time(请求时间)</p>
</li>
<li><p>Expires 指定的时间可以是相对文件的最后访问时间(Atime)或者修改时间(MTime)，而max-age相对对的是文档的请求时间(Atime)</p>
</li>
<li><p>如果值为 no-***,那么每次都会访问服务器。如果值为max-age，则在过期之前不会重复访问服务器。</p>
</li>
</ol>
<h4 id="73-js-操作获取和设置-cookie"><a href="#73-js-操作获取和设置-cookie" class="headerlink" title="73.js 操作获取和设置 cookie"></a>73.js 操作获取和设置 cookie</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; 创建cookie</span><br><span class="line">function setCookie(name, value, expires, path, domain, secure) &#123;</span><br><span class="line">  var cookieText &#x3D; encodeURIComponent(name) + &#39;&#x3D;&#39; + encodeURIComponent(value);</span><br><span class="line">  if (expires instanceof Date) &#123;</span><br><span class="line">    cookieText +&#x3D; &#39;; expires&#x3D;&#39; + expires;</span><br><span class="line">  &#125;</span><br><span class="line">  if (path) &#123;</span><br><span class="line">    cookieText +&#x3D; &quot;; path&#x3D;&quot; + path   &#125;</span><br><span class="line">  if (domain) &#123;</span><br><span class="line">    cookieText +&#x3D; &#39;; domain&#x3D;&#39; + domain;</span><br><span class="line">  &#125;</span><br><span class="line">  if (secure) &#123;</span><br><span class="line">  &#125;</span><br><span class="line">  document.cookie &#x3D; cookieText;</span><br><span class="line">&#125;</span><br><span class="line">&#x2F;&#x2F; 获取cookie</span><br><span class="line">function getCookie(name) &#123;</span><br><span class="line">  var cookieName &#x3D; encodeURIComponent(name) + &#39;&#x3D;&#39;;</span><br><span class="line">  var cookieStart &#x3D; document.cookie.indexOf(cookieName);</span><br><span class="line">  var cookieValue &#x3D; null;</span><br><span class="line">  if (cookieStart &gt; -1) &#123;</span><br><span class="line">    var cookieEnd &#x3D; document.cookie.indexOf(&#39;;&#39;, cookieStart);</span><br><span class="line">    if (cookieEnd &#x3D;&#x3D; -1) &#123;</span><br><span class="line">      cookieEnd &#x3D; document.cookie.length;</span><br><span class="line">    &#125;</span><br><span class="line">    cookieValue &#x3D; decodeURIComponent(document.cookie.substring(cookieStart + cookieName.length, cookieEnd));</span><br><span class="line">  &#125;</span><br><span class="line">  return cookieValue;</span><br><span class="line">&#125;</span><br><span class="line">&#x2F;&#x2F; 删除cookie</span><br><span class="line">function unsetCookie(name) &#123;</span><br><span class="line">  document.cookie &#x3D; name + &quot;&#x3D; ; expires&#x3D;&quot; + new Date(0);</span><br></pre></td></tr></table></figure>]]></content>
  </entry>
  <entry>
    <title>前端学习笔记</title>
    <url>/2020/05/25/%E8%BF%99%E6%98%AF%E7%AC%AC%E4%B8%80%E7%AF%87%E6%96%87%E7%AB%A0/</url>
    <content><![CDATA[<h3 id="算术运算符"><a href="#算术运算符" class="headerlink" title="算术运算符"></a>算术运算符</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">1.	由于js中的小数和整数都是number类型，不存在类似整数除以整数还是整数的结论。</span><br><span class="line">2.	字符串和其他的数据使用+号运算，会连接成一个新的字符串。</span><br><span class="line">3.	字符串使用除了+以外的运算符：如果字符串本身是一个数字，那么会自动转成number进行运算，否则就会返回一个NaN的结果，表示这不是一个数字。NaN：not a number</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;script&gt; </span><br><span class="line">    alert(1234 &#x2F; 1000 * 1000); &#x2F;&#x2F; 1234</span><br><span class="line">  </span><br><span class="line">    var s &#x3D; &quot;12&quot;;</span><br><span class="line">     s -&#x3D; 10;</span><br><span class="line">    alert(s);  &#x2F;&#x2F; 2</span><br><span class="line"> </span><br><span class="line">    var s &#x3D; &quot;aa&quot;;</span><br><span class="line">    s -&#x3D; 10;</span><br><span class="line">    alert(s);  &#x2F;&#x2F; NaN       Not a Number 不是一个数字</span><br><span class="line"> </span><br><span class="line">    var s &#x3D; &quot;12&quot;;</span><br><span class="line">    s +&#x3D; 10;</span><br><span class="line">    alert(s);       &#x2F;&#x2F; 1210  </span><br><span class="line">&lt;&#x2F;script&gt;</span><br></pre></td></tr></table></figure>

<h3 id="自定义函数"><a href="#自定义函数" class="headerlink" title="自定义函数"></a>自定义函数</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function 函数名(形式参数)&#123;函数体&#125;</span><br><span class="line">调用函数：函数名(实际参数);</span><br></pre></td></tr></table></figure>

<h3 id="匿名函数"><a href="#匿名函数" class="headerlink" title="匿名函数"></a>匿名函数</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function(形式参数)&#123;函数体&#125;</span><br><span class="line">调用方式：将匿名函数赋值给一个变量，通过变量名调用函数</span><br><span class="line">定义函数并赋值给变量：var fn &#x3D; function(形式参数)&#123;函数体&#125;</span><br><span class="line">调用函数：fn(实际参数);</span><br></pre></td></tr></table></figure>

<h3 id="案例-轮播图"><a href="#案例-轮播图" class="headerlink" title="案例-轮播图"></a>案例-轮播图</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">说明1 : script 标签需要放在 body 标签之后.</span><br><span class="line"> </span><br><span class="line">说明2 : window.setInterval(“字符串函数名称()”, 时间毫秒数);</span><br><span class="line"> </span><br><span class="line">说明3 : window.setInterval(函数名称, 时间毫秒数);</span><br><span class="line"> </span><br><span class="line">说明4 : window.setInterval(匿名函数, 时间毫秒数);            推荐使用</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;head&gt;</span><br><span class="line">    &lt;meta charset&#x3D;&quot;UTF-8&quot;&gt;</span><br><span class="line">    &lt;title&gt;轮播图&lt;&#x2F;title&gt;</span><br><span class="line"> </span><br><span class="line">    &lt;style&gt;</span><br><span class="line"> </span><br><span class="line">        div &#123;</span><br><span class="line">            width: 80%;</span><br><span class="line">            margin: 50px auto;</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        img &#123;</span><br><span class="line">            width: 100%;</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">    &lt;&#x2F;style&gt;</span><br><span class="line"> </span><br><span class="line">&lt;&#x2F;head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">    &lt;div class&#x3D;&quot;container&quot;&gt;</span><br><span class="line">        &lt;img src&#x3D;&quot;..&#x2F;img&#x2F;01.jpg&quot; alt&#x3D;&quot;图片&quot;&gt;</span><br><span class="line">    &lt;&#x2F;div&gt;</span><br><span class="line">&lt;&#x2F;body&gt;</span><br></pre></td></tr></table></figure>

<p>实现一：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;script&gt;</span><br><span class="line"> </span><br><span class="line">    &#x2F;&#x2F; 需求 : 动态获取页面中的 img 标签, 然后修改 img 标签的 src 属性.</span><br><span class="line">    &#x2F;&#x2F; 1. 获取 img 标签</span><br><span class="line">    var img &#x3D; document.getElementById(&quot;img&quot;);</span><br><span class="line">    &#x2F;&#x2F; alert(img);</span><br><span class="line"> </span><br><span class="line">    &#x2F;&#x2F; 定义一个变量</span><br><span class="line">    var count &#x3D; 1;</span><br><span class="line"> </span><br><span class="line">    &#x2F;&#x2F; 1.2 定义一个函数</span><br><span class="line">    function changeImageSrc() &#123;</span><br><span class="line">        count++;</span><br><span class="line">        img.src &#x3D; &quot;..&#x2F;img&#x2F;0&quot;+count+&quot;.jpg&quot;;</span><br><span class="line"> </span><br><span class="line">        &#x2F;&#x2F; 判断</span><br><span class="line">        if (count &#x3D;&#x3D; 8) &#123;</span><br><span class="line">            count &#x3D; 0;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    &#x2F;&#x2F; 2. 循环切换图片</span><br><span class="line">    &#x2F;&#x2F; window.setInterval(函数, 时间毫秒); 在指定的时间毫秒间隔, 不断调用第一个参数传入的函数.</span><br><span class="line">    &#x2F;&#x2F; 调用方式一 :</span><br><span class="line">    &#x2F;&#x2F; window.setInterval(&quot;changeImageSrc()&quot;, 1000);</span><br><span class="line">    &#x2F;&#x2F; 调用方式二 :</span><br><span class="line">    window.setInterval(changeImageSrc, 1000);</span><br><span class="line"> </span><br><span class="line">&lt;&#x2F;script&gt;</span><br></pre></td></tr></table></figure>

<p>实现二：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;script&gt;</span><br><span class="line"> </span><br><span class="line">    &#x2F;&#x2F; 需求 : 动态获取页面中的 img 标签, 然后修改 img 标签的 src 属性.</span><br><span class="line">    &#x2F;&#x2F; 1. 获取 img 标签</span><br><span class="line">    var img &#x3D; document.getElementById(&quot;img&quot;);</span><br><span class="line">    &#x2F;&#x2F; alert(img);</span><br><span class="line"> </span><br><span class="line">    &#x2F;&#x2F; 定义一个变量</span><br><span class="line">    var count &#x3D; 1;</span><br><span class="line"> </span><br><span class="line">    &#x2F;&#x2F; 2. 循环切换图片</span><br><span class="line">    &#x2F;&#x2F; window.setInterval(匿名函数, 时间毫秒); 在指定的时间毫秒间隔, 不断调用第一个参数传入的匿名函数.</span><br><span class="line">    window.setInterval(function() &#123;</span><br><span class="line">        count++;</span><br><span class="line">        img.src &#x3D; &quot;..&#x2F;img&#x2F;0&quot;+count+&quot;.jpg&quot;;</span><br><span class="line"> </span><br><span class="line">        &#x2F;&#x2F; 判断</span><br><span class="line">        if (count &#x3D;&#x3D; 8) &#123;</span><br><span class="line">            count &#x3D; 0;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;, 1000);</span><br><span class="line"> </span><br><span class="line">&lt;&#x2F;script&gt;</span><br></pre></td></tr></table></figure>

<h3 id="数组去重？"><a href="#数组去重？" class="headerlink" title="数组去重？"></a>数组去重？</h3><p>js方法：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">var arr&#x3D;[&#39;12&#39;,&#39;32&#39;,&#39;89&#39;,&#39;12&#39;,&#39;12&#39;,&#39;78&#39;,&#39;12&#39;,&#39;32&#39;];</span><br><span class="line">    &#x2F;&#x2F; 最简单数组去重法</span><br><span class="line">    function unique1(array)&#123;</span><br><span class="line">        var n &#x3D; []; &#x2F;&#x2F;一个新的临时数组</span><br><span class="line">        for(var i &#x3D; 0; i &lt; array.length; i++)&#123; &#x2F;&#x2F;遍历当前数组</span><br><span class="line">            if (n.indexOf(array[i]) &#x3D;&#x3D; -1)</span><br><span class="line">                n.push(array[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        return n;</span><br><span class="line">    &#125;</span><br><span class="line">    arr&#x3D;unique1(arr);</span><br><span class="line">    &#x2F;&#x2F; 速度最快， 占空间最多（空间换时间）</span><br><span class="line">    function unique2(array)&#123;</span><br><span class="line">        var n &#x3D; &#123;&#125;, r &#x3D; [], type;</span><br><span class="line">        for (var i &#x3D; 0; i &lt; array.length; i++) &#123;</span><br><span class="line">            type &#x3D; typeof array[i];</span><br><span class="line">            if (!n[array[i]]) &#123;</span><br><span class="line">                n[array[i]] &#x3D; [type];</span><br><span class="line">                r.push(array[i]);</span><br><span class="line">            &#125; else if (n[array[i]].indexOf(type) &lt; 0) &#123;</span><br><span class="line">                n[array[i]].push(type);</span><br><span class="line">                r.push(array[i]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return r;</span><br><span class="line">    &#125;</span><br><span class="line">    &#x2F;&#x2F;数组下标判断法</span><br><span class="line">    function unique3(array)&#123;</span><br><span class="line">        var n &#x3D; [array[0]]; &#x2F;&#x2F;结果数组</span><br><span class="line">        for(var i &#x3D; 1; i &lt; array.length; i++) &#123; &#x2F;&#x2F;从第二项开始遍历</span><br><span class="line">            if (array.indexOf(array[i]) &#x3D;&#x3D; i) </span><br><span class="line">                n.push(array[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        return n;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>es6方法：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">arr&#x3D;[...new Set(arr)];</span><br><span class="line">es6方法数组去重，第二种方法</span><br><span class="line">function dedupe(array) &#123;</span><br><span class="line">  return Array.from(new Set(array));       &#x2F;&#x2F;Array.from()能把set结构转换为数组</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="export和export-default的区别？"><a href="#export和export-default的区别？" class="headerlink" title="export和export default的区别？"></a>export和export default的区别？</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">export default  xxx</span><br><span class="line">import xxx from &#39;.&#x2F;&#39;</span><br><span class="line"></span><br><span class="line">export xxx</span><br><span class="line">import &#123;xxx&#125; from &#39;.&#x2F;&#39;</span><br></pre></td></tr></table></figure>

<h3 id="说一下闭包？"><a href="#说一下闭包？" class="headerlink" title="说一下闭包？"></a>说一下闭包？</h3><p>闭包的实质是因为函数嵌套而形成的作用域链</p>
<p>闭包的定义即：函数 <code>A</code> 内部有一个函数 <code>B</code>，函数 <code>B</code> 可以访问到函数 <code>A</code> 中的变量，那么函数 <code>B</code> 就是闭包</p>
<h3 id="说一下继承的几种方式及优缺点？"><a href="#说一下继承的几种方式及优缺点？" class="headerlink" title="说一下继承的几种方式及优缺点？"></a>说一下继承的几种方式及优缺点？</h3><ol>
<li>借用构造函数继承，使用call或apply方法，将父对象的构造函数绑定在子对象上</li>
<li>原型继承，将子对象的prototype指向父对象的一个实例</li>
<li>组合继承</li>
</ol>
<p>原型链继承的缺点</p>
<ul>
<li>字面量重写原型会中断关系，使用引用类型的原型，并且子类型还无法给超类型传递参数。</li>
</ul>
<p>借用构造函数（类式继承）</p>
<ul>
<li>借用构造函数虽然解决了刚才两种问题，但没有原型，则复用无从谈起。</li>
</ul>
<p>组合式继承</p>
<ul>
<li>组合式继承是比较常用的一种继承方法，其背后的思路是使用原型链实现对原型属性和方法的继承，而通过借用构造函数来实现对实例属性的继承。这样，既通过在原型上定义方法实现了函数复用，又保证每个实例都有它自己的属性。</li>
</ul>
<h3 id="target、currentTarget的区别？"><a href="#target、currentTarget的区别？" class="headerlink" title="target、currentTarget的区别？"></a>target、currentTarget的区别？</h3><p>currentTarget当前所绑定事件的元素</p>
<p>target当前被点击的元素</p>
<h3 id="说一下宏任务和微任务？"><a href="#说一下宏任务和微任务？" class="headerlink" title="说一下宏任务和微任务？"></a>说一下宏任务和微任务？</h3><ol>
<li>宏任务：当前调用栈中执行的任务称为宏任务。（主代码快，定时器等等）。</li>
<li>微任务： 当前（此次事件循环中）宏任务执行完，在下一个宏任务开始之前需要执行的任务为微任务。（可以理解为回调事件，promise.then，proness.nextTick等等）。</li>
<li>宏任务中的事件放在callback queue中，由事件触发线程维护；微任务的事件放在微任务队列中，由js引擎线程维护。</li>
</ol>
<h3 id="说一下事件代理？"><a href="#说一下事件代理？" class="headerlink" title="说一下事件代理？"></a>说一下事件代理？</h3><p>事件委托是指将事件绑定到目标元素的父元素上，利用冒泡机制触发该事件</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ulEl.addEventListener(&#39;click&#39;, function(e)&#123;</span><br><span class="line">    var target &#x3D; event.target || event.srcElement;</span><br><span class="line">    if(!!target &amp;&amp; target.nodeName.toUpperCase() &#x3D;&#x3D;&#x3D; &quot;LI&quot;)&#123;</span><br><span class="line">        console.log(target.innerHTML);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;, false);</span><br></pre></td></tr></table></figure>

<h3 id="如何中断ajax请求？"><a href="#如何中断ajax请求？" class="headerlink" title="如何中断ajax请求？"></a>如何中断ajax请求？</h3><p>一种是设置超时时间让ajax自动断开，另一种是手动停止ajax请求，其核心是调用XML对象的abort方法，ajax.abort()</p>
<h3 id="页面渲染html的过程？"><a href="#页面渲染html的过程？" class="headerlink" title="页面渲染html的过程？"></a>页面渲染html的过程？</h3><p>浏览器渲染页面的一般过程：</p>
<p>1.浏览器解析html源码，然后创建一个 DOM树。并行请求 css/image/js在DOM树中，每一个HTML标签都有一个对应的节点，并且每一个文本也都会有一个对应的文本节点。DOM树的根节点就是 documentElement，对应的是html标签。</p>
<p>2.浏览器解析CSS代码，计算出最终的样式数据。构建CSSOM树。对CSS代码中非法的语法它会直接忽略掉。解析CSS的时候会按照如下顺序来定义优先级：浏览器默认设置 &lt; 用户设置 &lt; 外链样式 &lt; 内联样式 &lt; html中的style。</p>
<p>3.DOM Tree + CSSOM –&gt; 渲染树（rendering tree）。渲染树和DOM树有点像，但是是有区别的。</p>
<p>DOM树完全和html标签一一对应，但是渲染树会忽略掉不需要渲染的元素，比如head、display:none的元素等。而且一大段文本中的每一个行在渲染树中都是独立的一个节点。渲染树中的每一个节点都存储有对应的css属性。</p>
<p>4.一旦渲染树创建好了，浏览器就可以根据渲染树直接把页面绘制到屏幕上。</p>
<p>以上四个步骤并不是一次性顺序完成的。如果DOM或者CSSOM被修改，以上过程会被重复执行。实际上，CSS和JavaScript往往会多次修改DOM或者CSSOM。</p>
<h3 id="用js递归的方式写1到100求和？"><a href="#用js递归的方式写1到100求和？" class="headerlink" title="用js递归的方式写1到100求和？"></a>用js递归的方式写1到100求和？</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function add(num1,num2)&#123;</span><br><span class="line">	var num &#x3D; num1+num2;</span><br><span class="line">        if(num2+1&gt;100)&#123;</span><br><span class="line">	 return num;</span><br><span class="line">	&#125;else&#123;</span><br><span class="line">	  return add(num,num2+1)</span><br><span class="line">        &#125;</span><br><span class="line"> &#125;</span><br><span class="line">var sum &#x3D;add(1,2);</span><br></pre></td></tr></table></figure>

<h3 id="说一下标签的用法"><a href="#说一下标签的用法" class="headerlink" title="说一下标签的用法"></a>说一下<label>标签的用法</h3><p>label标签主要是方便鼠标点击使用，扩大可点击的范围，增强用户操作体验</p>
<h3 id="遍历A节点的父节点下的所有子节点"><a href="#遍历A节点的父节点下的所有子节点" class="headerlink" title="遍历A节点的父节点下的所有子节点"></a>遍历A节点的父节点下的所有子节点</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;script&gt;</span><br><span class="line">    var b&#x3D;document.getElementById(&quot;a&quot;).parentNode.children;</span><br><span class="line">    console.log(b)</span><br><span class="line">&lt;&#x2F;script&gt;</span><br></pre></td></tr></table></figure>

<h3 id="清除浮动的几种方式，及原理？"><a href="#清除浮动的几种方式，及原理？" class="headerlink" title="清除浮动的几种方式，及原理？"></a>清除浮动的几种方式，及原理？</h3><blockquote>
<p>清除浮动简单，但这题要引出的是BFC，BFC也是必考的基础知识点</p>
</blockquote>
<ul>
<li><code>::after / &lt;br&gt; / clear: both</code></li>
<li>创建父级 <code>BFC</code>(overflow:hidden)</li>
<li>父级设置高度</li>
</ul>
<blockquote>
<p><em>BFC （</em>块级格式化上下文<em>）</em>，是一个独立的渲染区域，让处于 <code>BFC</code> 内部的元素与外部的元素相互隔离，使内外元素的定位不会相互影响。</p>
</blockquote>
<p><em>触发条件:</em></p>
<ul>
<li>根元素</li>
<li><code>position: absolute/fixed</code></li>
<li><code>display: inline-block / table</code></li>
<li><code>float</code> 元素</li>
<li><code>ovevflow !== visible</code></li>
</ul>
<h3 id="画一个三角形？"><a href="#画一个三角形？" class="headerlink" title="画一个三角形？"></a>画一个三角形？</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.a&#123;</span><br><span class="line">            width: 0;</span><br><span class="line">            height: 0;</span><br><span class="line">            border-width: 100px;</span><br><span class="line">            border-style: solid;</span><br><span class="line">            border-color: transparent #0099CC transparent transparent;</span><br><span class="line">            transform: rotate(90deg); &#x2F;*顺时针旋转90°*&#x2F;</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line">&lt;div class&#x3D;&quot;a&quot;&gt;&lt;&#x2F;div&gt;</span><br></pre></td></tr></table></figure>

<h3 id="说一下盒模型？"><a href="#说一下盒模型？" class="headerlink" title="说一下盒模型？"></a>说一下盒模型？</h3><blockquote>
<p>盒模型是css中重要的基础知识，也是必考的基础知识</p>
</blockquote>
<p>盒模型的组成，由里向外content,padding,border,margin.</p>
<p>在IE盒子模型中，width表示content+padding+border这三个部分的宽度</p>
<p>在标准的盒子模型中，width指content部分的宽度</p>
<p>box-sizing的使用</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">box-sizing: content-box 是W3C盒子模型</span><br><span class="line">box-sizing: border-box 是IE盒子模型</span><br></pre></td></tr></table></figure>

<p>box-sizing的默认属性是content-box</p>
<h3 id="画一条0-5px的直线？"><a href="#画一条0-5px的直线？" class="headerlink" title="画一条0.5px的直线？"></a>画一条0.5px的直线？</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">height: 1px;</span><br><span class="line">transform: scale(0.5);</span><br></pre></td></tr></table></figure>

<h3 id="1rem、1em、1vh、1px各自代表的含义？"><a href="#1rem、1em、1vh、1px各自代表的含义？" class="headerlink" title="1rem、1em、1vh、1px各自代表的含义？"></a>1rem、1em、1vh、1px各自代表的含义？</h3><blockquote>
<p>rem</p>
</blockquote>
<p>rem是全部的长度都相对于根元素<html>元素。通常做法是给html元素设置一个字体大小，然后其他元素的长度单位就为rem。</p>
<blockquote>
<p>em</p>
</blockquote>
<ul>
<li>子元素字体大小的em是相对于父元素字体大小</li>
<li>元素的width/height/padding/margin用em的话是相对于该元素的font-size</li>
</ul>
<blockquote>
<p>vw/vh</p>
</blockquote>
<p>全称是 Viewport Width 和 Viewport Height，视窗的宽度和高度，相当于 屏幕宽度和高度的 1%，不过，处理宽度的时候%单位更合适，处理高度的 话 vh 单位更好。</p>
<blockquote>
<p>px</p>
</blockquote>
<p>px像素（Pixel）。相对长度单位。像素px是相对于显示器屏幕分辨率而言的。</p>
<p>一般电脑的分辨率有{1920*1024}等不同的分辨率</p>
<p>1920*1024 前者是屏幕宽度总共有1920个像素,后者则是高度为1024个像素</p>
<h3 id="css水平、垂直居中的写法，请至少写出4种？"><a href="#css水平、垂直居中的写法，请至少写出4种？" class="headerlink" title="css水平、垂直居中的写法，请至少写出4种？"></a>css水平、垂直居中的写法，请至少写出4种？</h3><p><em>水平居中</em></p>
<ul>
<li>行内元素: <code>text-align: center</code></li>
<li>块级元素: <code>margin: 0 auto</code></li>
<li>position:absolute +left:50%+ transform:translateX(-50%)</li>
<li><code>display:flex + justify-content: center</code></li>
</ul>
<p><em>垂直居中</em></p>
<ul>
<li>设置line-height 等于height</li>
<li>position：absolute +top:50%+ transform:translateY(-50%)</li>
<li><code>display:flex + align-items: center</code></li>
<li>display:table+display:table-cell + vertical-align: middle;</li>
</ul>
<h3 id="calc-support-media各自的含义及用法？"><a href="#calc-support-media各自的含义及用法？" class="headerlink" title="calc, support, media各自的含义及用法？"></a>calc, support, media各自的含义及用法？</h3><p>@support主要是用于检测浏览器是否支持CSS的某个属性，其实就是条件判断，如果支持某个属性，你可以写一套样式，如果不支持某个属性，你也可以提供另外一套样式作为替补。</p>
<p>calc() 函数用于动态计算长度值。 calc()函数支持 “+”, “-“, “*”, “/“ 运算；</p>
<p>@media 查询，你可以针对不同的媒体类型定义不同的样式。</p>
]]></content>
  </entry>
</search>
